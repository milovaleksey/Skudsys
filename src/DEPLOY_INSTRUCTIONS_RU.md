# 🚀 Инструкции по развертыванию системы ТюмГУ

## Текущая конфигурация

✅ **Режим:** HTTP с внешним доступом (без reverse proxy)  
✅ **База данных:** MySQL с паролями по умолчанию  
✅ **Доступ:** С любого компьютера в сети  
✅ **Автоматизация:** Полная (один скрипт)  

---

## 🎯 ЗАПУСК СИСТЕМЫ (2 команды)

```bash
# 1. Подготовка
chmod +x make-executable.sh && ./make-executable.sh

# 2. Развертывание
./deploy-external.sh
```

**ВСЁ!** Система автоматически:
- Проверит зависимости (Node.js, MySQL, Nginx)
- Установит все пакеты
- Создаст БД с чистого листа
- Настроит Nginx для внешнего доступа
- Запустит все сервисы

⏱️ **Время:** 5-10 минут

---

## 📋 Что происходит при установке

### Шаг 1: Проверка зависимостей
- Node.js 16+
- npm
- MySQL 5.7+
- Nginx 1.18+

### Шаг 2: Установка пакетов
- Frontend зависимости (React, Tailwind, etc.)
- Backend зависимости (Express, MySQL, JWT, etc.)

### Шаг 3: База данных
- **ПОЛНАЯ ОЧИСТКА** существующей БД (если есть)
- Создание БД `utmn_security`
- Создание пользователя `utmn_admin` с паролем `utmn_admin123`
- Импорт схемы (11 таблиц)
- Создание администратора `admin` с паролем `admin123`
- Создание 5 базовых ролей

### Шаг 4: Backend настройка
- Генерация JWT секретов
- Настройка на прослушивание `0.0.0.0:3000` (все интерфейсы)
- Создание файла `.env` с паролями

### Шаг 5: Nginx настройка
- Копирование конфига для внешнего доступа
- Проксирование Frontend (:5173) и Backend (:3000)
- Настройка CORS для тестирования

### Шаг 6: Systemd сервисы
- `utmn-backend` - Backend API (автозапуск)
- `utmn-frontend` - Vite dev server (автозапуск)
- `nginx` - Web сервер

### Шаг 7: Запуск
- MySQL
- Backend (с ожиданием 3 сек)
- Frontend (с ожиданием 3 сек)
- Nginx

### Шаг 8: Проверка
- Health check API
- Статус всех сервисов

---

## 🌐 Доступ к системе

### Узнать IP адрес сервера

```bash
hostname -I
# Результат, например: 192.168.1.100
```

### Открыть в браузере

```
http://192.168.1.100
```

### Вход

```
Логин:  admin
Пароль: admin123
```

---

## ✅ Проверка работы

```bash
# Статус всех сервисов
./check-status.sh
```

Должны работать:
- ✅ MySQL
- ✅ utmn-backend
- ✅ utmn-frontend
- ✅ Nginx

Должны быть открыты порты:
- ✅ 80 (Nginx)
- ✅ 3000 (Backend)
- ✅ 5173 (Frontend dev)
- ✅ 3306 (MySQL)

---

## 🔧 Управление системой

### Просмотр статуса

```bash
sudo systemctl status utmn-backend utmn-frontend nginx mysql
```

### Перезапуск сервисов

```bash
# Всё сразу
sudo systemctl restart utmn-backend utmn-frontend nginx

# По отдельности
sudo systemctl restart utmn-backend
sudo systemctl restart utmn-frontend
sudo systemctl restart nginx
```

### Просмотр логов

```bash
# Backend (в реальном времени)
journalctl -u utmn-backend -f

# Frontend (в реальном времени)
journalctl -u utmn-frontend -f

# Nginx ошибки
sudo tail -f /var/log/nginx/utmn-security-error.log

# Последние 100 строк Backend
journalctl -u utmn-backend -n 100 --no-pager
```

### Остановка/запуск

```bash
# Остановка
sudo systemctl stop utmn-backend utmn-frontend

# Запуск
sudo systemctl start utmn-backend utmn-frontend
```

---

## 🗄️ Работа с базой данных

### Подключение к MySQL

```bash
mysql -u utmn_admin -putmn_admin123 utmn_security
```

### Просмотр таблиц

```bash
mysql -u utmn_admin -putmn_admin123 utmn_security -e "SHOW TABLES;"
```

### Просмотр пользователей

```bash
mysql -u utmn_admin -putmn_admin123 utmn_security -e "SELECT id, username, email, role FROM users;"
```

### Статистика данных

```bash
mysql -u utmn_admin -putmn_admin123 utmn_security -e "
  SELECT 'Пользователи' as Таблица, COUNT(*) as Записей FROM users
  UNION ALL SELECT 'Студенты', COUNT(*) FROM students
  UNION ALL SELECT 'Сотрудники', COUNT(*) FROM employees
  UNION ALL SELECT 'Парковка', COUNT(*) FROM parking_records;
"
```

### Пересоздание БД (УДАЛИТ ВСЕ ДАННЫЕ!)

```bash
./reset-database.sh
```

---

## 📊 Импорт тестовых данных

### Генерация 300 парковочных записей

```bash
mysql -u utmn_admin -putmn_admin123 utmn_security << 'EOF'
INSERT INTO parking_lots (name, capacity, current_occupancy) VALUES
('Главный корпус', 150, 0),
('Библиотека', 80, 0),
('Общежитие №1', 50, 0);

DELIMITER $$
CREATE PROCEDURE generate_parking()
BEGIN
  DECLARE i INT DEFAULT 1;
  WHILE i <= 300 DO
    INSERT INTO parking_records (lot_id, license_plate, owner_name, entry_time)
    VALUES (
      FLOOR(1 + RAND() * 3),
      CONCAT(CHAR(FLOOR(65 + RAND() * 26)), LPAD(FLOOR(RAND() * 1000), 3, '0'), 
             CHAR(FLOOR(65 + RAND() * 26)), CHAR(FLOOR(65 + RAND() * 26)), 
             LPAD(FLOOR(RAND() * 100), 2, '0')),
      CONCAT('Владелец ', i),
      DATE_SUB(NOW(), INTERVAL FLOOR(RAND() * 365) DAY)
    );
    SET i = i + 1;
  END WHILE;
END$$
DELIMITER ;

CALL generate_parking();
DROP PROCEDURE generate_parking;
EOF
```

---

## 🔍 Проверка API

### Health Check

```bash
curl http://localhost/health
# Должен вернуть: {"success":true,"message":"OK"}
```

### Тест авторизации

```bash
curl -X POST http://localhost/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### Получение списка студентов (с токеном)

```bash
# Сначала получаем токен
TOKEN=$(curl -s -X POST http://localhost/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | jq -r '.data.token')

# Затем делаем запрос с токеном
curl -H "Authorization: Bearer $TOKEN" http://localhost/v1/students
```

---

## 🔥 Настройка Firewall (опционально)

Если система не доступна извне:

```bash
# Установка UFW
sudo apt install ufw

# ВАЖНО! Сначала разрешите SSH, чтобы не потерять доступ
sudo ufw allow 22/tcp

# Разрешите HTTP
sudo ufw allow 80/tcp

# Включите firewall
sudo ufw enable

# Проверьте статус
sudo ufw status verbose
```

---

## 🐛 Решение проблем

### Backend не запускается

```bash
# Смотрим подробные логи
journalctl -u utmn-backend -n 100 --no-pager

# Проверяем порт 3000
sudo netstat -tulpn | grep 3000

# Проверяем файл .env
cat backend/.env

# Перезапускаем
sudo systemctl restart utmn-backend
```

### Frontend не запускается

```bash
# Смотрим логи
journalctl -u utmn-frontend -n 100 --no-pager

# Проверяем порт 5173
sudo netstat -tulpn | grep 5173

# Перезапускаем
sudo systemctl restart utmn-frontend
```

### Nginx ошибки

```bash
# Проверяем конфигурацию
sudo nginx -t

# Смотрим логи
sudo tail -n 50 /var/log/nginx/error.log

# Перезапускаем
sudo systemctl restart nginx
```

### MySQL не подключается

```bash
# Проверяем статус
sudo systemctl status mysql

# Пробуем подключиться
mysql -u utmn_admin -putmn_admin123 utmn_security

# Если не получается, пересоздаем пользователя
mysql -u root -p
# В MySQL консоли:
# DROP USER 'utmn_admin'@'localhost';
# CREATE USER 'utmn_admin'@'localhost' IDENTIFIED BY 'utmn_admin123';
# GRANT ALL PRIVILEGES ON utmn_security.* TO 'utmn_admin'@'localhost';
# FLUSH PRIVILEGES;
```

### Не работает доступ извне

```bash
# Проверяем IP адрес
hostname -I

# Проверяем доступность с сервера
curl http://localhost

# Проверяем Nginx слушает на всех интерфейсах
sudo netstat -tulpn | grep :80

# Проверяем firewall
sudo ufw status

# Пингуем с другого компьютера
# ping 192.168.1.100 (ваш IP)
```

---

## 📝 Пароли по умолчанию

### Веб-интерфейс
```
URL:    http://ВАШ_IP
Логин:  admin
Пароль: admin123
```

### MySQL
```
База:        utmn_security
Пользователь: utmn_admin
Пароль:      utmn_admin123
```

⚠️ **ОБЯЗАТЕЛЬНО СМЕНИТЕ ПАРОЛИ ПОСЛЕ ВХОДА!**

---

## 🎯 Что дальше?

После успешного развертывания:

1. ✅ Войдите в систему (http://ВАШ_IP)
2. ✅ Смените пароль администратора в настройках
3. ✅ Создайте пользователей с разными ролями
4. ✅ Импортируйте данные студентов и сотрудников
5. ✅ Сгенерируйте тестовые данные парковки
6. ✅ Протестируйте все модули
7. ✅ Настройте дашборды

Для production:
8. ⚠️ Настройте HTTPS
9. ⚠️ Настройте reverse proxy
10. ⚠️ Измените все пароли
11. ⚠️ Настройте автоматические бэкапы
12. ⚠️ Настройте мониторинг

---

## 📚 Документация

- `START_HERE_QUICK.md` - Супер-краткая инструкция
- `DEPLOY_NOW.md` - Подробное руководство
- `README_DEPLOYMENT.md` - Полная документация
- `WHATS_READY.md` - Что уже готово
- `API_ENDPOINTS.md` - Документация API
- `ROLES.md` - Система ролей

---

## ✅ Checklist развертывания

- [ ] Выполнен `./make-executable.sh`
- [ ] Выполнен `./deploy-external.sh`
- [ ] Все сервисы запущены (проверено через `./check-status.sh`)
- [ ] Открывается веб-интерфейс (http://ВАШ_IP)
- [ ] Успешный вход как admin
- [ ] Пароль администратора изменен
- [ ] Импортированы тестовые данные
- [ ] Все модули работают

---

**Готово! Система работает и доступна в вашей сети.**

🚀 **Начните с:** `./deploy-external.sh`  
🔍 **Проверьте:** `./check-status.sh`  
🌐 **Откройте:** `http://ВАШ_IP`
