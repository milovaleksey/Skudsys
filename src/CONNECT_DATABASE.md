# 🔌 Подключение фронтенда к базе данных

## ✅ Что уже готово:

1. ✅ **Backend API** - полностью настроен и готов к работе
2. ✅ **База данных MySQL** - 11 таблиц с полной структурой
3. ✅ **API клиент** - создан `/lib/api.ts` с всеми методами
4. ✅ **Конфигурация** - `.env` файлы для настройки

---

## 🚀 Инструкция по запуску с базой данных

### Шаг 1: Настройка Backend

#### 1.1 Перейдите в папку backend:
```bash
cd /root/utmn-security/backend
```

#### 1.2 Создайте файл `.env` в папке `backend`:
```bash
nano .env
```

#### 1.3 Добавьте следующие настройки:
```env
# Server Configuration
PORT=3000
NODE_ENV=production
API_VERSION=v1

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=utmn_security
DB_PASSWORD=ваш_пароль_mysql
DB_NAME=utmn_security_db

# JWT Configuration
JWT_SECRET=ваш_секретный_ключ_jwt_минимум_32_символа
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=ваш_секретный_ключ_refresh_минимум_32_символа
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:5173,http://localhost:3001
CORS_CREDENTIALS=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

**Важно!** Замените:
- `ваш_пароль_mysql` - на реальный пароль от MySQL
- `ваш_секретный_ключ_jwt_минимум_32_символа` - на случайную строку (можно сгенерировать: `openssl rand -base64 32`)
- `ваш_секретный_ключ_refresh_минимум_32_символа` - на другую случайную строку

#### 1.4 Установите зависимости backend (если ещё не установлены):
```bash
npm install
```

#### 1.5 Запустите backend:
```bash
# Для development
npm run dev

# Для production
npm start
```

Вы должны увидеть:
```
✅ Подключено к MySQL
🚀 Сервер запущен на порту 3000
📡 API: http://localhost:3000/v1
🏥 Health: http://localhost:3000/health
```

---

### Шаг 2: Настройка Frontend

#### 2.1 Вернитесь в корневую папку проекта:
```bash
cd /root/utmn-security
```

#### 2.2 Проверьте файл `.env` в корне проекта:
```bash
cat .env
```

Должно быть:
```env
VITE_API_BASE_URL=http://localhost:3000
VITE_API_VERSION=v1
VITE_APP_NAME=Системы безопасности инфраструктуры ТюмГУ
VITE_APP_VERSION=1.0.0
VITE_NODE_ENV=development
```

#### 2.3 Если работаете на production сервере, измените URL:
```bash
nano .env
```

Измените на:
```env
VITE_API_BASE_URL=http://ваш_домен_или_ip:3000
```

---

### Шаг 3: Проверка подключения

#### 3.1 Проверьте, работает ли backend:
```bash
curl http://localhost:3000/health
```

Ответ должен быть:
```json
{
  "success": true,
  "message": "API работает",
  "timestamp": "2026-01-21T...",
  "version": "v1"
}
```

#### 3.2 Запустите frontend:
```bash
npm run dev
```

---

### Шаг 4: Подготовка данных в базе

#### 4.1 Создайте тестового пользователя-администратора:

Подключитесь к MySQL:
```bash
mysql -u utmn_security -p utmn_security_db
```

Выполните SQL:
```sql
-- Проверяем, есть ли роль admin
SELECT * FROM roles WHERE name = 'admin';

-- Если нет, создаём
INSERT INTO roles (name, display_name, description, permissions, is_system, created_at)
VALUES (
  'admin',
  'Администратор',
  'Полный доступ ко всем функциям системы',
  '["dashboard","dashboard-builder","passes","location","analytics","parking","storage","foreign-students","students","employees","users-settings","roles-settings","user-logs"]',
  1,
  NOW()
);

-- Создаём администратора (пароль: admin123)
INSERT INTO users (username, full_name, email, password_hash, role_id, auth_type, is_active, created_at)
VALUES (
  'admin',
  'Администратор Системы',
  'admin@utmn.ru',
  '$2b$10$rBV2ifqQvZWqBG8Lx1c3rOJ5kUqKZxQxJQPQKQYRzBOZqJQZqJQZq', -- admin123
  (SELECT id FROM roles WHERE name = 'admin'),
  'local',
  1,
  NOW()
);
```

**Примечание:** Пароль `admin123` уже зашифрован через bcrypt. Для создания своего пароля используйте:
```bash
node -e "console.log(require('bcrypt').hashSync('ваш_пароль', 10))"
```

#### 4.2 Проверьте созданного пользователя:
```sql
SELECT u.*, r.name as role_name 
FROM users u 
JOIN roles r ON u.role_id = r.id 
WHERE u.username = 'admin';
```

---

## 🔄 Переключение с Mock-данных на реальную БД

### Вариант 1: Постепенное переключение (РЕКОМЕНДУЕТСЯ)

Сейчас в системе используются mock-данные. Для переключения на реальную БД:

#### 1. Авторизация уже готова:
В `/lib/api.ts` уже настроен метод `authApi.login()`, который работает с реальным API.

#### 2. Обновление компонентов:
Постепенно обновляйте компоненты, заменяя mock-данные на API вызовы:

```typescript
// Было (mock):
const [students, setStudents] = useState(mockStudents);

// Стало (реальная БД):
const [students, setStudents] = useState([]);
const [loading, setLoading] = useState(true);

useEffect(() => {
  async function loadStudents() {
    setLoading(true);
    const response = await studentsApi.getAll({ page: 1, limit: 100 });
    if (response.success) {
      setStudents(response.data);
    }
    setLoading(false);
  }
  loadStudents();
}, []);
```

### Вариант 2: Режим разработки/production

Можно оставить mock-данные для разработки и использовать БД для production:

```typescript
const USE_MOCK_DATA = import.meta.env.VITE_NODE_ENV === 'development';

if (USE_MOCK_DATA) {
  // Используем mock-данные
  setStudents(mockStudents);
} else {
  // Используем реальную БД
  const response = await studentsApi.getAll();
  setStudents(response.data);
}
```

---

## 📋 Какие данные нужно подготовить

### Обязательные таблицы для запуска:

1. **`roles`** - роли пользователей (admin, security, manager, operator, viewer)
2. **`users`** - пользователи системы (минимум 1 администратор)

### Таблицы для отчётов:

3. **`students`** - данные студентов
4. **`employees`** - данные сотрудников
5. **`access_logs`** - логи доступа/проходов
6. **`parking_lots`** - парковки
7. **`parking_records`** - записи о припаркованных авто
8. **`storage_items`** - вещи на хранении

### Опциональные:

9. **`dashboards`** - сохранённые дашборды
10. **`widgets`** - виджеты дашбордов
11. **`data_sources`** - источники данных

---

## 🧪 Тестирование API

### Проверка авторизации:
```bash
curl -X POST http://localhost:3000/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123",
    "authType": "local"
  }'
```

### Проверка получения списка студентов:
```bash
# Сначала получите токен из предыдущего запроса
TOKEN="ваш_jwt_токен"

curl http://localhost:3000/v1/students \
  -H "Authorization: Bearer $TOKEN"
```

---

## 🐛 Возможные проблемы

### Проблема 1: Backend не запускается
**Причина:** MySQL не запущен или неверные учётные данные

**Решение:**
```bash
# Проверьте, запущен ли MySQL
sudo systemctl status mysql

# Запустите, если не запущен
sudo systemctl start mysql

# Проверьте подключение
mysql -u utmn_security -p
```

### Проблема 2: CORS ошибка
**Причина:** Frontend и Backend на разных доменах

**Решение:** В файле `backend/.env` добавьте URL фронтенда:
```env
CORS_ORIGIN=http://localhost:5173,http://ваш_ip:3001
```

### Проблема 3: 401 Unauthorized
**Причина:** Токен истёк или неверный

**Решение:** Проверьте настройки JWT в `backend/.env`

---

## 📊 Импорт данных

### Импорт студентов из CSV:

Создайте файл `import_students.sql`:
```sql
LOAD DATA LOCAL INFILE '/path/to/students.csv'
INTO TABLE students
FIELDS TERMINATED BY ',' 
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(full_name, student_id, faculty, course, group_name, email, phone);
```

### Импорт сотрудников:
```sql
LOAD DATA LOCAL INFILE '/path/to/employees.csv'
INTO TABLE employees
FIELDS TERMINATED BY ',' 
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(full_name, employee_id, department, position, email, phone);
```

---

## ✅ Checklist готовности

- [ ] Backend запущен и отвечает на `/health`
- [ ] MySQL база данных создана
- [ ] Таблицы созданы через `database/schema.sql`
- [ ] Создан минимум 1 администратор
- [ ] Созданы все роли (admin, security, manager, operator, viewer)
- [ ] Frontend подключается к backend (проверьте консоль браузера)
- [ ] Авторизация работает
- [ ] Импортированы данные студентов/сотрудников (опционально)

---

## 🎯 Следующие шаги

1. **Запустите backend** (см. Шаг 1)
2. **Создайте администратора** (см. Шаг 4.1)
3. **Запустите frontend** (см. Шаг 2.3)
4. **Войдите в систему** (admin / admin123)
5. **Начните импортировать данные** через веб-интерфейс или SQL

---

Всё готово к работе! 🚀

Если нужна помощь с импортом данных или возникнут проблемы - дайте знать!
