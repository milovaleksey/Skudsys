# 📦 Установка зависимостей - Инструкция

## ✅ Проблема решена!

Я исправил конфликт зависимостей:
- ✅ Удалил `react-day-picker` (конфликтовал с date-fns)
- ✅ Переписал `calendar.tsx` на чистом React
- ✅ Добавил `overrides` для принудительной версии date-fns
- ✅ Оставил только `react-datepicker` (используется в отчетах)

---

## 🚀 Установка - 3 простых шага

### Шаг 1: Очистить старые зависимости

```bash
rm -rf node_modules package-lock.json
```

**Для Windows:**
```cmd
rmdir /s /q node_modules
del package-lock.json
```

### Шаг 2: Установить зависимости

```bash
npm install
```

**Должно работать без ошибок!** ✅

### Шаг 3: Запустить проект

```bash
npm run dev
```

Откройте http://localhost:5173

---

## 📊 Что было изменено

### 1. package.json

**Удалено:**
```json
"react-day-picker": "^8.10.1"  // ❌ Конфликтовал
```

**Добавлено:**
```json
"overrides": {
  "date-fns": "^3.6.0"  // ✅ Принудительная версия
}
```

**Оставлено:**
```json
"date-fns": "^3.6.0",
"react-datepicker": "^6.9.0"  // ✅ Совместим с date-fns 3.x
```

### 2. components/ui/calendar.tsx

Переписан на чистом React без зависимостей от react-day-picker.

**Возможности:**
- ✅ Выбор даты
- ✅ Навигация по месяцам
- ✅ Выделение сегодняшней даты
- ✅ Русские названия месяцев и дней
- ✅ Совместим с существующим кодом

---

## 🐛 Если всё равно ошибка

### Вариант 1: Использовать --legacy-peer-deps

```bash
rm -rf node_modules package-lock.json
npm install --legacy-peer-deps
```

### Вариант 2: Использовать --force

```bash
rm -rf node_modules package-lock.json
npm install --force
```

### Вариант 3: Использовать yarn

```bash
# Установить yarn
npm install -g yarn

# Очистить
rm -rf node_modules package-lock.json yarn.lock

# Установить
yarn install

# Запустить
yarn dev
```

### Вариант 4: Использовать pnpm

```bash
# Установить pnpm
npm install -g pnpm

# Очистить
rm -rf node_modules package-lock.json pnpm-lock.yaml

# Установить
pnpm install

# Запустить
pnpm dev
```

---

## ✅ Проверка успешной установки

После установки проверьте:

```bash
npm list date-fns
```

**Ожидаемый результат:**
```
utmn-security-system@1.0.0
└── date-fns@3.6.0
```

Не должно быть `date-fns@4.x.x`!

---

## 📚 Используемые пакеты для дат

| Пакет | Версия | Используется в |
|-------|--------|----------------|
| date-fns | 3.6.0 | Форматирование дат |
| react-datepicker | 6.9.0 | Отчеты (PassesReport, EmployeesReport, StudentsReport) |
| Calendar (свой) | - | UI компонент (если нужен) |

---

## 🔄 Для production развертывания

Скрипт `deploy.sh` уже обновлен и автоматически использует fallback:

```bash
# В deploy.sh
if ! npm install 2>/dev/null; then
    npm install --legacy-peer-deps
fi
```

Ничего дополнительно делать не нужно!

---

## 🎯 Backend установка

Backend не зависит от frontend зависимостей:

```bash
cd backend
npm install
cp .env.example .env
# Отредактировать .env
node src/scripts/initDatabase.js
npm run dev
```

---

## 🆘 Помощь

### Проверить версии:

```bash
node --version   # Должно быть v18+ или v20+
npm --version    # Должно быть v9+
```

### Очистить кеш npm:

```bash
npm cache clean --force
rm -rf ~/.npm
```

### Полная переустановка:

```bash
# 1. Удалить всё
rm -rf node_modules package-lock.json

# 2. Очистить кеш
npm cache clean --force

# 3. Установить заново
npm install
```

---

## ✅ Готово!

Теперь просто выполните:

```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```

И всё заработает! 🎉

---

**Дата:** 20.01.2026  
**Статус:** ✅ Полностью исправлено  
**Версия:** 1.0
