# Frontend - Система безопасности ТюмГУ

React + TypeScript приложение для системы безопасности инфраструктуры Тюменского государственного университета.

---

## 🛠 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev сервер
- **Tailwind CSS** - стилизация
- **React Router** - маршрутизация
- **Radix UI** - компоненты UI
- **Recharts** - графики и аналитика
- **Lucide React** - иконки

---

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

### Сборка production

```bash
npm run build
```

Собранное приложение будет в папке `dist/`

### Предпросмотр production сборки

```bash
npm run preview
```

---

## 📁 Структура проекта

```
frontend/
├── components/           # React компоненты
│   ├── ui/              # UI компоненты (кнопки, инпуты и т.д.)
│   ├── LoginPage.tsx    # Страница авторизации
│   ├── MainPage.tsx     # Главная страница
│   └── ...              # Другие страницы
├── contexts/            # React контексты
│   └── AuthContext.tsx  # Контекст авторизации
├── lib/                 # Библиотеки и утилиты
│   └── api.ts          # API клиент
├── styles/              # CSS файлы
│   └── globals.css     # Глобальные стили
├── public/              # Статические файлы
│   └── logo.svg        # Логотип
├── App.tsx              # Главный компонент
├── main.tsx             # Точка входа
└── index.html           # HTML шаблон
```

---

## 🔧 Конфигурация

### Environment Variables

Создайте файл `.env` в корне frontend:

```env
# API Base URL (пусто для proxy через Vite)
VITE_API_BASE_URL=

# API Version
VITE_API_VERSION=v1
```

### Proxy (Development)

В режиме разработки Vite проксирует запросы к backend:

- `/v1/*` → `http://localhost:3000`
- `/health` → `http://localhost:3000`

Настройка в `vite.config.ts`

### Production

В production запросы идут через Nginx:

```nginx
location /v1/ {
  proxy_pass http://localhost:3000;
}
```

---

## 🎨 Фирменный стиль

### Цвета ТюмГУ

- **Основной:** `#00aeef` (синий)
- **Темы:** Определены в `styles/globals.css` через CSS переменные

### Компоненты

Используются компоненты из Radix UI с кастомной стилизацией через Tailwind CSS.

Все UI компоненты находятся в `components/ui/`

---

## 📡 API

Клиент для работы с backend API находится в `lib/api.ts`

### Пример использования

```typescript
import { usersApi } from '@/lib/api';

// Получить всех пользователей
const response = await usersApi.getAll({ page: 1, limit: 10 });

if (response.success) {
  console.log(response.data);
}
```

### Доступные API

- `authApi` - авторизация
- `usersApi` - пользователи
- `rolesApi` - роли
- `studentsApi` - студенты
- `employeesApi` - сотрудники
- `parkingApi` - парковка
- `analyticsApi` - аналитика

---

## 🔐 Авторизация

Авторизация реализована через `AuthContext`:

```typescript
import { useAuth } from '@/contexts/AuthContext';

function MyComponent() {
  const { user, login, logout, hasPermission } = useAuth();

  // Проверка прав доступа
  if (hasPermission('users.view')) {
    // ...
  }
}
```

### Роли

- `admin` - администратор
- `security` - служба безопасности
- `teacher` - преподаватель
- `student` - студент
- `guest` - гость

---

## 🧪 Линтинг

```bash
npm run lint
```

---

## 📦 Сборка для production

```bash
# Сборка
npm run build

# Развертывание на сервере
scp -r dist/* user@server:/var/www/utmn-security/frontend/
```

---

## 🐛 Отладка

### DevTools

Откройте браузер DevTools (F12) для просмотра:
- Network запросов
- Console логов
- React DevTools

### Логирование API

При проблемах с API проверьте:
1. Network tab - статус запросов
2. Console - ошибки API
3. localStorage - наличие токена

```javascript
// В консоли браузера
localStorage.getItem('auth_token')
```

---

## 📚 Документация

- [RESTRUCTURE.md](../RESTRUCTURE.md) - Реструктуризация проекта
- [BUGFIX_AUTH_TOKEN.md](../BUGFIX_AUTH_TOKEN.md) - Исправление авторизации
- [API_DOCUMENTATION.md](../API_DOCUMENTATION.md) - API документация

---

## 🤝 Разработка

### Добавление новой страницы

1. Создайте компонент в `components/`:
```typescript
// components/MyPage.tsx
export function MyPage() {
  return <div>My Page</div>;
}
```

2. Добавьте маршрут в `App.tsx`:
```typescript
<Route path="/my-page" element={<MyPage />} />
```

### Добавление нового API endpoint

1. Добавьте метод в `lib/api.ts`:
```typescript
export const myApi = {
  async getData() {
    return apiClient.get('/my-endpoint');
  }
};
```

---

**Версия:** 1.0.0  
**Дата:** 25.01.2026
