# Структура исходного кода системы безопасности инфраструктуры ТюмГУ

## 📁 Архитектура проекта

```
/
├── App.tsx                          # Главный компонент приложения
├── contexts/
│   └── AuthContext.tsx              # Контекст авторизации и управления ролями
├── components/
│   ├── MainPage.tsx                 # Главная страница с дашбордом
│   ├── LoginPage.tsx                # Страница авторизации
│   ├── Sidebar.tsx                  # Боковое меню навигации
│   ├── RoleSwitcher.tsx             # Компонент тестирования ролей
│   ├── UsersSettingsPage.tsx        # Управление пользователями
│   ├── RolesManagementPage.tsx      # Управление ролями
│   └── ui/                          # UI компоненты
│       ├── button.tsx
│       ├── input.tsx
│       ├── dialog.tsx
│       ├── alert-dialog.tsx
│       ├── select.tsx
│       ├── tabs.tsx
│       ├── badge.tsx
│       ├── switch.tsx
│       ├── checkbox.tsx
│       ├── label.tsx
│       ├── textarea.tsx
│       └── scroll-area.tsx
└── styles/
    └── globals.css                  # Глобальные стили
```

## 🔐 Система авторизации и ролей

### AuthContext.tsx

**Описание:** Центральный контекст для управления авторизацией, пользователями и ролями.

**Основные типы:**

```typescript
// Интерфейс роли
interface Role {
  id: string;                    // Уникальный ID роли
  name: string;                  // Системное имя (admin, security, custom_role)
  displayName: string;           // Отображаемое название
  description: string;           // Описание роли
  permissions: string[];         // Массив прав доступа
  isSystem: boolean;             // Системная роль (нельзя удалить)
  createdAt: string;            // Дата создания
  updatedAt?: string;           // Дата обновления
}

// Интерфейс пользователя
interface User {
  id: number;
  username: string;
  fullName: string;
  email: string;
  role: string;                  // Ссылка на role.name
  authType: 'local' | 'sso';
  avatar?: string;
  createdAt: string;
  lastLogin?: string;
  isActive: boolean;
}
```

**Доступные права (permissions):**

| ID | Название | Категория |
|---|---|---|
| `dashboard` | Главная панель | Основные |
| `passes` | Отчет о проходах | Безопасность |
| `location` | Местонахождение людей | Безопасность |
| `analytics` | Аналитика | Отчеты |
| `parking` | Парковочная система | Инфраструктура |
| `storage` | Система хранения вещей | Инфраструктура |
| `foreign-students` | Отчет по иностранным студентам | Отчеты |
| `students` | Отчет по студентам | Отчеты |
| `employees` | Отчет по сотрудникам | Отчеты |
| `users-settings` | Управление пользователями | Администрирование |
| `roles-settings` | Управление ролями | Администрирование |

**Методы контекста:**

```typescript
// Авторизация
login(username: string, password: string, authType: 'local' | 'sso'): Promise<void>

// Выход
logout(): void

// Проверка прав
hasPermission(permission: string): boolean
hasAnyPermission(permissions: string[]): boolean

// Управление пользователем
updateUser(user: User): void

// Управление ролями
addRole(role: Omit<Role, 'id' | 'createdAt'>): Role
updateRole(roleId: string, updates: Partial<Role>): void
deleteRole(roleId: string): void
getRoleByName(name: string): Role | undefined
```

**Системные роли (по умолчанию):**

1. **admin** - Администратор
   - Все права доступа
   - Системная роль (нельзя удалить)

2. **security** - Безопасность
   - dashboard, passes, location, parking, storage

3. **manager** - Менеджер
   - dashboard, analytics, students, employees, foreign-students

4. **operator** - Оператор
   - dashboard, passes, location, students, employees

5. **viewer** - Наблюдатель
   - dashboard, analytics

## 📄 Основные компоненты

### RolesManagementPage.tsx

**Описание:** Страница управления ролями с возможностью создания, редактирования и удаления.

**Функционал:**
- ✅ Создание новых кастомных ролей
- ✅ Редактирование существующих ролей
- ✅ Удаление кастомных ролей (системные защищены)
- ✅ Выбор прав доступа с группировкой по категориям
- ✅ Статистика: всего ролей, системных, кастомных
- ✅ Валидация: уникальность имен, обязательные поля

**Основные состояния:**

```typescript
const [isAddDialogOpen, setIsAddDialogOpen] = useState(false);
const [isEditDialogOpen, setIsEditDialogOpen] = useState(false);
const [isDeleteDialogOpen, setIsDeleteDialogOpen] = useState(false);
const [selectedRole, setSelectedRole] = useState<Role | null>(null);
const [formData, setFormData] = useState({
  name: '',
  displayName: '',
  description: '',
  permissions: [] as string[],
});
```

**Ключевые функции:**

```typescript
// Создание роли
handleSaveNewRole(): void

// Редактирование роли
handleEditRole(role: Role): void
handleSaveEditRole(): void

// Удаление роли
handleDeleteRole(): void

// Работа с правами
togglePermission(permissionId: string): void
toggleCategoryPermissions(category: string): void
```

### UsersSettingsPage.tsx

**Описание:** Страница управления пользователями с тремя вкладками.

**Вкладки:**
1. **Управление пользователями** - список пользователей, создание, редактирование
2. **Статистика** - графики и метрики по пользователям
3. **Управление ролями** - встроенный RolesManagementPage

**Функционал:**
- ✅ Создание новых пользователей
- ✅ Редактирование существующих пользователей
- ✅ Удаление пользователей
- ✅ Активация/деактивация пользователей
- ✅ Поиск по имени, email, username
- ✅ Фильтрация по роли
- ✅ Экспорт данных (CSV, JSON)

**Основные состояния:**

```typescript
const [users, setUsers] = useState<User[]>(initialUsers);
const [searchQuery, setSearchQuery] = useState('');
const [roleFilter, setRoleFilter] = useState<string>('all');
const [isAddDialogOpen, setIsAddDialogOpen] = useState(false);
const [isEditDialogOpen, setIsEditDialogOpen] = useState(false);
const [isDeleteDialogOpen, setIsDeleteDialogOpen] = useState(false);
const [selectedUser, setSelectedUser] = useState<User | null>(null);
const [formData, setFormData] = useState({...});
```

### MainPage.tsx

**Описание:** Главная страница с дашбордом, статистикой и боковым меню.

**Функционал:**
- ✅ Отображение статистики (студенты, сотрудники, общежития и т.д.)
- ✅ Боковое меню с фильтрацией по правам доступа
- ✅ Информация о текущем пользователе
- ✅ Переключатель тестирования ролей (dev mode)

**Разделы меню:**

```typescript
const menuItems = [
  { id: 'passes', label: 'Отчет о проходах', icon: ClipboardList, permission: 'passes' },
  { id: 'location', label: 'Местонахождение людей', icon: MapPin, permission: 'location' },
  { id: 'analytics', label: 'Аналитика', icon: BarChart3, permission: 'analytics' },
  { id: 'parking', label: 'Парковочная система', icon: Car, permission: 'parking' },
  { id: 'storage', label: 'Система хранения вещей', icon: Package, permission: 'storage' },
  { id: 'foreign-students', label: 'Отчет по иностранным студентам', icon: Globe, permission: 'foreign-students' },
  { id: 'students', label: 'Отчет по студентам', icon: GraduationCap, permission: 'students' },
  { id: 'employees', label: 'Отчет по сотрудникам', icon: Users, permission: 'employees' },
  { id: 'users-settings', label: 'Настройки пользователей', icon: Settings, permission: 'users-settings' },
];
```

### LoginPage.tsx

**Описание:** Страница авторизации с поддержкой двух типов входа.

**Типы авторизации:**
1. **Локальная** - логин + пароль
2. **SSO_UTMN** - Single Sign-On университета

**Форма:**

```typescript
const [authType, setAuthType] = useState<'local' | 'sso'>('local');
const [formData, setFormData] = useState({
  username: '',
  password: '',
});
```

## 🎨 UI Компоненты

### Основные UI компоненты

**Button** - кнопки с вариантами:
- `default` - основная кнопка
- `outline` - контурная кнопка
- `ghost` - прозрачная кнопка
- `destructive` - красная кнопка (удаление)

**Dialog** - модальные окна для форм

**AlertDialog** - диалоги подтверждения действий

**Select** - выпадающие списки

**Tabs** - вкладки для группировки контента

**Badge** - метки и теги

**Switch** - переключатели on/off

**Checkbox** - чекбоксы для выбора

**Input** - текстовые поля ввода

**Textarea** - многострочные текстовые поля

**Label** - подписи к полям форм

**ScrollArea** - область с прокруткой

## 🔧 Примеры использования

### Проверка прав доступа

```typescript
import { useAuth } from '../contexts/AuthContext';

function MyComponent() {
  const { hasPermission } = useAuth();

  if (!hasPermission('analytics')) {
    return <div>Нет доступа</div>;
  }

  return <div>Аналитика</div>;
}
```

### Создание кастомной роли

```typescript
const { addRole } = useAuth();

const newRole = addRole({
  name: 'custom_parking_manager',
  displayName: 'Менеджер парковки',
  description: 'Управление парковочной системой',
  permissions: ['dashboard', 'parking'],
  isSystem: false,
});
```

### Фильтрация меню по правам

```typescript
const { hasPermission } = useAuth();

const visibleMenuItems = menuItems.filter(item => 
  hasPermission(item.permission)
);
```

## 🎯 Сценарии использования

### Сценарий 1: Создание роли "Диспетчер общежития"

1. Администратор открывает "Настройки пользователей" → вкладка "Управление ролями"
2. Нажимает "Создать роль"
3. Заполняет:
   - Системное имя: `dormitory_dispatcher`
   - Отображаемое название: `Диспетчер общежития`
   - Описание: `Контроль проходов и хранения вещей в общежитии`
   - Права: `dashboard`, `passes`, `location`, `storage`
4. Сохраняет роль
5. Назначает эту роль сотрудникам общежития

### Сценарий 2: Ограничение доступа для стажера

1. Создается роль "Стажер-аналитик" с правами:
   - `dashboard`, `analytics` (только просмотр)
2. Создается новый пользователь со стажерской ролью
3. Стажер видит только дашборд и аналитику
4. Остальные разделы меню скрыты

### Сценарий 3: Экспорт списка пользователей

1. Открыть "Настройки пользователей"
2. Применить фильтры (по роли, поиск)
3. Нажать "Экспорт" → выбрать формат (CSV/JSON)
4. Файл автоматически скачивается

## 🚀 Технологический стек

- **React 18** - библиотека для UI
- **TypeScript** - типизация
- **Tailwind CSS v4** - стилизация
- **Radix UI** - доступные UI компоненты
- **Lucide React** - иконки
- **Sonner** - уведомления (toast)
- **Recharts** - графики и диаграммы

## 📊 Корпоративный стиль ТюмГУ

**Основной цвет:** `#00aeef` (голубой)

**Использование в коде:**

```typescript
// Кнопки
className="bg-[#00aeef] hover:bg-[#008ac4]"

// Иконки
className="text-[#00aeef]"

// Фон
className="bg-[#00aeef]/10"
```

## 🔐 Безопасность

### Защита системных ролей

```typescript
const deleteRole = (roleId: string) => {
  const role = roles.find(r => r.id === roleId);
  if (role?.isSystem) {
    throw new Error('Системные роли нельзя удалять');
  }
  setRoles(roles.filter(r => r.id !== roleId));
};
```

### Валидация прав доступа

```typescript
const hasPermission = (permission: string): boolean => {
  if (!user) return false;
  const role = getRoleByName(user.role);
  return role ? role.permissions.includes(permission) : false;
};
```

## 📝 Будущие улучшения

- [ ] Интеграция с backend API
- [ ] Реальная авторизация через SSO университета
- [ ] История изменений ролей и пользователей
- [ ] Массовое назначение ролей
- [ ] Импорт пользователей из CSV
- [ ] Двухфакторная аутентификация
- [ ] Логирование действий пользователей
- [ ] Уведомления по email при изменении прав

## 📞 Контакты для разработчиков

При возникновении вопросов или предложений по улучшению системы обращайтесь к документации в файлах:
- `/ROLES.md` - документация по ролевой модели
- `/USER_GUIDE.md` - руководство пользователя

---

**Версия документации:** 1.0  
**Дата обновления:** 19.01.2026  
**ТюмГУ - Системы безопасности инфраструктуры**
