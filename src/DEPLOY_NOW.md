# 🚀 Быстрое развертывание с внешним доступом

## Автоматическая установка (3 команды)

```bash
# 1. Сделать скрипты исполняемыми
chmod +x make-executable.sh
./make-executable.sh

# 2. Запустить автоматическое развертывание
./deploy-external.sh

# 3. Открыть в браузере
# http://ВАШ_IP_АДРЕС
# Логин: admin
# Пароль: admin123
```

## Что делает скрипт автоматически

✅ Проверяет все зависимости (Node.js, MySQL, Nginx)  
✅ Устанавливает все npm пакеты  
✅ **Полностью очищает и пересоздает БД с паролями по умолчанию**  
✅ Создает администратора (admin/admin123)  
✅ Настраивает Nginx для внешнего доступа  
✅ Создает systemd сервисы для автозапуска  
✅ Запускает все сервисы  

**Время выполнения:** 5-10 минут

## Пароли по умолчанию

### Вход в систему
```
Логин:  admin
Пароль: admin123
```

### База данных MySQL
```
База данных: utmn_security
Пользователь: utmn_admin
Пароль:      utmn_admin123
```

## После установки

### Узнать IP адрес сервера
```bash
hostname -I
# или
ip addr show
```

### Открыть в браузере
```
http://ВАШ_IP_АДРЕС
```

### Проверить статус
```bash
sudo systemctl status utmn-backend utmn-frontend nginx
```

### Просмотр логов
```bash
# Backend
journalctl -u utmn-backend -f

# Frontend
journalctl -u utmn-frontend -f

# Nginx
sudo tail -f /var/log/nginx/utmn-security-error.log
```

## Управление сервисами

```bash
# Перезапуск всех сервисов
sudo systemctl restart utmn-backend utmn-frontend nginx

# Остановка
sudo systemctl stop utmn-backend utmn-frontend

# Запуск
sudo systemctl start utmn-backend utmn-frontend
```

## Доступ с других компьютеров в сети

### Windows (проверка доступа)
```cmd
ping ВАШ_IP_АДРЕС
```

Откройте браузер: `http://ВАШ_IP_АДРЕС`

### Linux/Mac (проверка доступа)
```bash
curl http://ВАШ_IP_АДРЕС/health
```

## Настройка Firewall (если не работает доступ извне)

```bash
# Установка UFW
sudo apt install ufw

# Разрешить SSH (ВАЖНО! Сделайте это первым)
sudo ufw allow 22/tcp

# Разрешить HTTP
sudo ufw allow 80/tcp

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status
```

## Если что-то пошло не так

### Пересоздать только БД
```bash
./reset-database.sh
```

### Посмотреть подробные логи
```bash
# Backend не запускается
journalctl -u utmn-backend -n 100 --no-pager

# Frontend не запускается
journalctl -u utmn-frontend -n 100 --no-pager

# Nginx ошибки
sudo nginx -t
sudo tail -f /var/log/nginx/error.log
```

### Проверить порты
```bash
sudo netstat -tulpn | grep -E ':(80|3000|5173|3306)'
```

### Перезапустить всё заново
```bash
sudo systemctl stop utmn-backend utmn-frontend nginx
sudo systemctl start mysql
sleep 2
sudo systemctl start utmn-backend
sleep 3
sudo systemctl start utmn-frontend
sleep 2
sudo systemctl start nginx
```

## Полезные команды

### Проверка БД
```bash
# Подключение к MySQL
mysql -u utmn_admin -putmn_admin123 utmn_security

# Просмотр таблиц
mysql -u utmn_admin -putmn_admin123 utmn_security -e "SHOW TABLES;"

# Просмотр пользователей
mysql -u utmn_admin -putmn_admin123 utmn_security -e "SELECT id, username, email, role FROM users;"
```

### Проверка API
```bash
# Health check
curl http://localhost/health

# Тест логина
curl -X POST http://localhost/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

## Доступ к системе

После успешного развертывания система будет доступна по адресам:

**Локально на сервере:**
- http://localhost
- http://127.0.0.1

**С других компьютеров в сети:**
- http://ВАШ_IP_АДРЕС (например: http://192.168.1.100)

**API endpoints:**
- http://ВАШ_IP_АДРЕС/v1/auth/login
- http://ВАШ_IP_АДРЕС/health

## Следующие шаги

1. ✅ Войдите в систему (admin/admin123)
2. ✅ Смените пароль администратора в настройках
3. ✅ Создайте дополнительных пользователей
4. ✅ Импортируйте данные студентов и сотрудников
5. ✅ Настройте регулярные бэкапы БД
6. ⚠️  Для production настройте HTTPS и reverse proxy

## ⚠️ Важно для безопасности

Эта конфигурация предназначена для **тестирования и разработки**.

Для production сервера:
- Настройте HTTPS (SSL сертификаты)
- Используйте reverse proxy
- Измените все пароли по умолчанию
- Настройте firewall правильно
- Включите автоматические обновления
- Настройте мониторинг и логирование

---

**Поддержка:**
Если возникли проблемы, проверьте логи:
```bash
journalctl -u utmn-backend -f
```
