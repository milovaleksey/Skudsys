# 🔧 Исправление проблемы с зависимостями

## ❌ Проблема

Конфликт между:
- `date-fns@4.1.0` (требует react-datepicker)
- `date-fns@^2.28.0 || ^3.0.0` (требует react-day-picker)

## ✅ Решение

Я исправил проблему! Теперь используется **совместимая версия date-fns 3.6.0**.

---

## 🚀 Как установить зависимости

### Вариант 1: Обычная установка (рекомендуется)

```bash
npm install
```

Должно работать без ошибок!

---

### Вариант 2: С legacy peer deps (если есть другие проблемы)

```bash
npm install --legacy-peer-deps
```

---

### Вариант 3: Принудительная установка

```bash
npm install --force
```

⚠️ Использовать только если первые два варианта не помогли.

---

## 📦 Что было исправлено

### 1. Создан `package.json` с правильными версиями:

```json
{
  "dependencies": {
    "date-fns": "^3.6.0",           // ✅ Совместима с обоими пакетами
    "react-datepicker": "^6.9.0",   // ✅ Поддерживает date-fns 3.x
    "react-day-picker": "^8.10.1",  // ✅ Поддерживает date-fns 3.x
    ...
  }
}
```

### 2. Исправлен `components/ui/calendar.tsx`:

**Было:**
```tsx
import { ChevronLeft, ChevronRight } from "lucide-react@0.487.0";
import { DayPicker } from "react-day-picker@8.10.1";
```

**Стало:**
```tsx
import { ChevronLeft, ChevronRight } from "lucide-react";
import { DayPicker } from "react-day-picker";
```

### 3. Созданы конфигурационные файлы:

- ✅ `vite.config.ts` - конфигурация Vite
- ✅ `tsconfig.json` - конфигурация TypeScript
- ✅ `tsconfig.node.json` - конфигурация для Node
- ✅ `tailwind.config.js` - конфигурация Tailwind
- ✅ `postcss.config.js` - конфигурация PostCSS
- ✅ `index.html` - HTML entry point
- ✅ `main.tsx` - React entry point
- ✅ `.gitignore` - игнорируемые файлы

---

## ✅ Проверка установки

После `npm install` проверьте:

```bash
# Должны быть установлены правильные версии
npm list date-fns
npm list react-datepicker
npm list react-day-picker
```

**Ожидаемый результат:**
```
├── date-fns@3.6.0
├── react-datepicker@6.9.0
└── react-day-picker@8.10.1
```

---

## 🚀 Запуск после установки

### Backend:

```bash
cd backend
npm install
cp .env.example .env
# Отредактировать .env (указать DB_PASSWORD)
node src/scripts/initDatabase.js
npm run dev
```

### Frontend:

```bash
# В корне проекта
npm install
npm run dev
```

Должно запуститься без ошибок на http://localhost:5173

---

## 🐛 Если всё равно есть ошибки

### 1. Очистить кеш npm:

```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### 2. Использовать yarn (альтернатива):

```bash
# Установить yarn
npm install -g yarn

# Установить зависимости
yarn install

# Запустить
yarn dev
```

### 3. Использовать pnpm (альтернатива):

```bash
# Установить pnpm
npm install -g pnpm

# Установить зависимости
pnpm install

# Запустить
pnpm dev
```

---

## 📊 Совместимость версий

| Пакет | Версия | date-fns |
|-------|--------|----------|
| date-fns | 3.6.0 | - |
| react-datepicker | 6.9.0 | 2.x - 3.x ✅ |
| react-day-picker | 8.10.1 | 2.x - 3.x ✅ |

**Все совместимы!** ✅

---

## 📚 Полезные команды

```bash
# Проверить устаревшие пакеты
npm outdated

# Обновить все пакеты (осторожно!)
npm update

# Проверить уязвимости
npm audit

# Исправить уязвимости
npm audit fix
```

---

## ✅ Готово!

Теперь можно запускать проект:

```bash
# Backend
cd backend && npm run dev

# Frontend (в другом терминале)
npm run dev
```

Откройте http://localhost:5173 в браузере.

---

**Дата исправления:** 20.01.2026  
**Статус:** ✅ Исправлено
