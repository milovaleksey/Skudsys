# ✅ Миграция в /frontend - Инструкция

## 🎯 Что сделано

Создана профессиональная monorepo структура:

```
/
├── frontend/              ← Новая структура Frontend
│   ├── package.json      ✅ Создан
│   ├── vite.config.ts    ✅ Создан
│   ├── tsconfig.json     ✅ Создан
│   ├── tailwind.config.js ✅ Создан
│   ├── index.html        ✅ Создан
│   ├── .gitignore        ✅ Создан
│   └── README.md         ✅ Создан
│
├── backend/               ← Backend (уже существует)
│   └── ...
│
└── package-monorepo.json  ✅ Создан (для управления monorepo)
```

---

## 🚀 Завершение миграции

### Вариант 1: Автоматический (Рекомендуется)

```bash
# Дать права на выполнение
chmod +x migrate-to-frontend.sh

# Запустить миграцию
./migrate-to-frontend.sh
```

Скрипт автоматически:
1. ✅ Скопирует все исходники в `/frontend`
2. ✅ Предложит установить зависимости
3. ✅ Покажет статистику файлов

---

### Вариант 2: Ручной

```bash
# 1. Копирование файлов
cp App.tsx frontend/
cp main.tsx frontend/
cp -r components frontend/
cp -r contexts frontend/
cp -r lib frontend/
cp -r styles frontend/

# Создание public если нужно
mkdir -p frontend/public
cp public/logo.svg frontend/public/ 2>/dev/null || true

# 2. Установка зависимостей
cd frontend
npm install

# 3. Проверка
npm run dev
```

---

## ✅ Проверка работоспособности

### 1. Запустите dev сервер

```bash
cd frontend
npm run dev
```

Должно открыться на `http://localhost:5173`

### 2. Проверьте что работает

- ✅ Страница загружается
- ✅ Можно залогиниться (admin / Admin2025)
- ✅ Все страницы доступны
- ✅ API запросы работают

### 3. Соберите production

```bash
npm run build
```

Должна создаться папка `frontend/dist/`

---

## 📦 Обновление корневого package.json

После успешной проверки замените корневой `package.json`:

```bash
# Сделайте бэкап
mv package.json package.json.old

# Используйте новый monorepo package.json
mv package-monorepo.json package.json
```

Теперь можно управлять всем проектом из корня:

```bash
# Установить все зависимости
npm run install:all

# Запустить frontend dev
npm run dev:frontend

# Запустить backend dev  
npm run dev:backend

# Собрать frontend
npm run build:frontend
```

---

## 🧹 Очистка (после успешной проверки)

После того как убедитесь что всё работает в `/frontend`, можно удалить старые файлы из корня:

```bash
# !!! ВНИМАНИЕ: делайте это только после проверки !!!

# Удалить исходники из корня
rm -rf components/
rm -rf contexts/
rm -rf lib/
rm -rf styles/
rm App.tsx
rm main.tsx
rm index.html

# Удалить старые конфигурационные файлы
rm vite.config.ts
rm tsconfig.json
rm tsconfig.node.json
rm tailwind.config.js
rm postcss.config.js
rm vite-env.d.ts

# Оставить только:
# - frontend/
# - backend/
# - документацию
# - скрипты развертывания
```

---

## 🔄 Обновление скриптов развертывания

### Новый скрипт сборки

```bash
chmod +x BUILD_AND_DEPLOY_NEW.sh
./BUILD_AND_DEPLOY_NEW.sh
```

### Обновление systemd сервиса

Если используется systemd, обновите пути в `systemd/utmn-security.service`:

```ini
[Service]
WorkingDirectory=/var/www/utmn-security/backend
ExecStart=/usr/bin/node /var/www/utmn-security/backend/src/server.js
```

### Обновление Nginx

Убедитесь что Nginx отдает файлы из правильной директории:

```nginx
location / {
  root /var/www/utmn-security/frontend;
  try_files $uri $uri/ /index.html;
}
```

---

## 📋 Checklist

- [ ] Запущен `./migrate-to-frontend.sh`
- [ ] Установлены зависимости в `/frontend`
- [ ] `npm run dev` работает в `/frontend`
- [ ] Логин работает
- [ ] API запросы работают
- [ ] `npm run build` создает `frontend/dist/`
- [ ] Обновлен корневой `package.json` → `package-monorepo.json`
- [ ] Обновлен `BUILD_AND_DEPLOY.sh` → `BUILD_AND_DEPLOY_NEW.sh`
- [ ] Удалены старые файлы из корня (опционально)
- [ ] Обновлены пути в Nginx/systemd (если нужно)

---

## 🎉 Готово!

После завершения у вас будет:

1. ✅ Профессиональная monorepo структура
2. ✅ Четкое разделение Frontend и Backend
3. ✅ Независимые зависимости для каждой части
4. ✅ Удобные команды для разработки и сборки
5. ✅ Готовность к CI/CD

---

## 🆘 Проблемы?

### Frontend не запускается

```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Ошибки импортов

Проверьте что все файлы скопированы:
```bash
ls -la frontend/components
ls -la frontend/contexts
ls -la frontend/lib
```

### API не работает

Проверьте что backend запущен:
```bash
cd backend
npm start
```

И что proxy настроен в `frontend/vite.config.ts`

---

## 📚 Документация

- [RESTRUCTURE.md](./RESTRUCTURE.md) - Подробная документация
- [frontend/README.md](./frontend/README.md) - Frontend документация
- [BUGFIX_AUTH_TOKEN.md](./BUGFIX_AUTH_TOKEN.md) - Решение проблем авторизации

---

**Дата:** 25.01.2026  
**Статус:** ✅ Готово к миграции
