# 🚀 БЫСТРЫЙ СТАРТ - 2 КОМАНДЫ

## Развертывание системы

```bash
# 1. Сделать скрипты исполняемыми
chmod +x make-executable.sh && ./make-executable.sh

# 2. Запустить автоматическое развертывание
./deploy-external.sh
```

**Время:** 5-10 минут  
**Что происходит:** Автоматическая установка всех зависимостей, создание БД, настройка Nginx, запуск сервисов

---

## Доступ к системе

После успешной установки:

```
🌐 Откройте в браузере: http://ВАШ_IP_АДРЕС

👤 Логин:  admin
🔑 Пароль: admin123
```

### Узнать IP адрес сервера:
```bash
hostname -I
```

---

## Проверка статуса

```bash
./check-status.sh
```

Показывает:
- ✅ Статус всех сервисов
- ✅ Открытые порты
- ✅ Подключение к БД
- ✅ Статистику данных
- ✅ Последние логи

---

## Управление

```bash
# Перезапуск всех сервисов
sudo systemctl restart utmn-backend utmn-frontend nginx

# Просмотр логов Backend
journalctl -u utmn-backend -f

# Просмотр логов Frontend  
journalctl -u utmn-frontend -f

# Проверка статуса
sudo systemctl status utmn-backend utmn-frontend nginx mysql
```

---

## Пароли по умолчанию

### Вход в систему:
- Логин: `admin`
- Пароль: `admin123`

### MySQL:
- База: `utmn_security`
- Пользователь: `utmn_admin`
- Пароль: `utmn_admin123`

---

## Если нужна помощь

### Полное руководство:
```bash
cat DEPLOY_NOW.md
```

### Пересоздать БД:
```bash
./reset-database.sh
```

### Логи при ошибках:
```bash
journalctl -u utmn-backend -n 100
```

---

## ⚠️ Важно

Эта установка для **тестирования и разработки**.

Для production:
- Настройте HTTPS
- Используйте reverse proxy  
- Смените все пароли
- Настройте firewall

---

**Готово!** Система полностью развернута и готова к работе.
