# 🚀 НАЧНИТЕ ЗДЕСЬ

Полная инструкция по реструктуризации и развертыванию системы UTMN Security.

---

## ⚡ Быстрый старт (3 команды)

```bash
# 1. Дать права на выполнение всем скриптам
chmod +x make-scripts-executable.sh && ./make-scripts-executable.sh

# 2. Скопировать файлы в /frontend
./copy-all-now.sh

# 3. Развернуть на сервер
sudo ./deploy-from-sync.sh
```

**Готово!** Система развернута. 🎉

---

## 📁 Что было сделано

### ✅ Создана новая структура проекта

```
/opt/utmn-security/
├── frontend/              ← Новая структура
│   ├── components/
│   ├── contexts/
│   ├── lib/
│   ├── styles/
│   ├── App.tsx
│   ├── main.tsx
│   ├── package.json
│   └── vite.config.ts
│
├── backend/               ← Как было
│   └── ...
│
└── Скрипты развертывания  ← Новые
    ├── deploy-from-sync.sh
    ├── quick-deploy.sh
    ├── rollback.sh
    └── status.sh
```

### ✅ Созданы скрипты

| Скрипт | Назначение |
|--------|------------|
| `copy-all-now.sh` | Копирование файлов в /frontend |
| `deploy-from-sync.sh` | Полное развертывание с резервными копиями |
| `quick-deploy.sh` | Быстрое развертывание без вопросов |
| `rollback.sh` | Откат к предыдущей версии |
| `status.sh` | Проверка статуса системы |

---

## 📖 Пошаговая инструкция

### Шаг 1: Подготовка

```bash
# Перейдите в папку проекта
cd /opt/utmn-security

# Дайте права на выполнение
chmod +x make-scripts-executable.sh
./make-scripts-executable.sh
```

### Шаг 2: Копирование файлов в /frontend

**Вариант A: Автоматический (рекомендуется)**

```bash
./copy-all-now.sh
```

**Вариант B: Вручную**

```bash
cp -r components frontend/
cp -r lib frontend/
cp -r styles frontend/
mkdir -p frontend/public
```

### Шаг 3: Проверка frontend

```bash
cd frontend
npm run build
```

Должно пройти успешно без ошибок.

### Шаг 4: Развертывание на сервер

**Вариант A: Полное развертывание (рекомендуется)**

```bash
sudo ./deploy-from-sync.sh
```

Что делает:
- ✅ Создает резервную копию
- ✅ Собирает frontend
- ✅ Копирует в /var/www/utmn-security
- ✅ Устанавливает зависимости
- ✅ Перезапускает сервисы
- ✅ Проверяет работоспособность
- ✅ Откатывается при ошибке

**Вариант B: Быстрое развертывание**

```bash
sudo ./quick-deploy.sh
```

Для опытных пользователей. Без резервных копий и подтверждений.

### Шаг 5: Проверка

```bash
./status.sh
```

Должны быть зеленые галочки для всех компонентов.

---

## 🎯 Типичные сценарии

### 1. Первое развертывание

```bash
# 1. Права
chmod +x make-scripts-executable.sh && ./make-scripts-executable.sh

# 2. Копирование
./copy-all-now.sh

# 3. Развертывание
sudo ./deploy-from-sync.sh

# 4. Проверка
./status.sh
```

### 2. Обновление кода

```bash
# После изменений в коде
sudo ./deploy-from-sync.sh  # или ./quick-deploy.sh

# Проверка
./status.sh
```

### 3. Откат при проблемах

```bash
# Откат к предыдущей версии
sudo ./rollback.sh

# Выберите версию и подтвердите
```

### 4. Мониторинг

```bash
# Статус системы
./status.sh

# Логи backend
sudo journalctl -u utmn-security -f

# Логи nginx
sudo tail -f /var/log/nginx/error.log
```

---

## 📊 Проверка работы

### 1. Статус сервисов

```bash
./status.sh
```

Должно показывать:
```
✅ Все компоненты работают (4/4)
```

### 2. API

```bash
curl http://localhost:3000/health
```

Ответ:
```json
{"status":"ok","timestamp":"2026-01-25T14:30:00.000Z"}
```

### 3. Frontend

Откройте в браузере: `http://your-server/`

Должна загрузиться страница логина.

### 4. Тестовый вход

- **Login:** admin
- **Password:** Admin2025

---

## 🔧 Конфигурация путей

Если ваши пути отличаются, отредактируйте переменные в скриптах:

```bash
# В каждом скрипте (deploy-from-sync.sh, etc.)
SYNC_DIR="/opt/utmn-security"        # Ваша папка синхронизации
PROD_DIR="/var/www/utmn-security"    # Production папка
SERVICE_NAME="utmn-security"         # Имя systemd сервиса
```

---

## 🆘 Решение проблем

### Ошибка: "vite failed to resolve import /main.tsx"

```bash
# Файлы не скопированы в /frontend
./copy-all-now.sh
cd frontend && npm run build
```

### Backend не запускается

```bash
# Проверьте логи
sudo journalctl -u utmn-security -n 50

# Проверьте зависимости
cd /var/www/utmn-security/backend
npm install
```

### Nginx показывает 502

```bash
# Проверьте что backend работает
curl http://localhost:3000/health

# Проверьте конфигурацию nginx
sudo nginx -t

# Проверьте логи
sudo tail -f /var/log/nginx/error.log
```

### "Permission denied" при запуске скрипта

```bash
# Дайте права
chmod +x script-name.sh

# Или всем сразу
./make-scripts-executable.sh
```

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| [START_HERE.md](./START_HERE.md) | **Этот файл** - главная инструкция |
| [QUICK_START_DEPLOYMENT.md](./QUICK_START_DEPLOYMENT.md) | Быстрая инструкция |
| [DEPLOYMENT_SCRIPTS.md](./DEPLOYMENT_SCRIPTS.md) | Подробно о каждом скрипте |
| [MIGRATION_COMPLETE.md](./MIGRATION_COMPLETE.md) | Миграция в /frontend |
| [FIX_BUILD_ERROR.md](./FIX_BUILD_ERROR.md) | Исправление ошибок сборки |
| [RESTRUCTURE.md](./RESTRUCTURE.md) | Детали реструктуризации |
| [frontend/README.md](./frontend/README.md) | Frontend документация |

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:

- ✅ Профессиональная monorepo структура
- ✅ Отдельные папки `/frontend` и `/backend`
- ✅ Автоматизированное развертывание
- ✅ Резервное копирование
- ✅ Откат при проблемах
- ✅ Мониторинг статуса

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи:**
   ```bash
   ./status.sh
   sudo journalctl -u utmn-security -n 100
   ```

2. **Откатитесь к предыдущей версии:**
   ```bash
   sudo ./rollback.sh
   ```

3. **Проверьте документацию:**
   - [DEPLOYMENT_SCRIPTS.md](./DEPLOYMENT_SCRIPTS.md)
   - [BUGFIX_AUTH_TOKEN.md](./BUGFIX_AUTH_TOKEN.md)

---

**Версия:** 1.0.0  
**Дата:** 25.01.2026  
**Система:** UTMN Security - Система безопасности инфраструктуры ТюмГУ
