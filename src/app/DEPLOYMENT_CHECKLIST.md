# ✅ Чеклист развертывания UTMN Security System

## 📋 Что было исправлено

### Проблема 1: MODULE_NOT_FOUND (utils/validation)
- ❌ **Ошибка:** `Cannot find module '../utils/validation'`
- ✅ **Исправлено:** Удален неиспользуемый импорт из `user.controller.js`

### Проблема 2: Ошибка валидации при логине
- ❌ **Ошибка:** Backend отклонял поле `authType`
- ✅ **Исправлено:** Добавлено `authType` в схему валидации Joi

### Проблема 3: Маршрут не найден
- ❌ **Ошибка:** Backend использовал `/v1/*`, curl использовал `/api/*`
- ✅ **Исправлено:** Добавлена поддержка обоих префиксов

---

## 🚀 Пошаговое развертывание

### Шаг 1: Перезапуск backend

```bash
# Перезапустите сервис
sudo systemctl restart utmn-security

# Проверьте статус (должен быть active/running)
sudo systemctl status utmn-security

# Если есть ошибки, проверьте логи
sudo journalctl -u utmn-security -n 100 --no-pager
```

**Ожидаемый вывод:**
```
● utmn-security.service - UTMN Security System Backend
   Loaded: loaded (/etc/systemd/system/utmn-security.service; enabled)
   Active: active (running) since ...
```

---

### Шаг 2: Проверка API

```bash
# Проверка 1: Health check (версия /api)
curl http://localhost:3000/api/health

# Проверка 2: Health check (версия /v1)
curl http://localhost:3000/v1/health

# Проверка 3: Health check (без префикса)
curl http://localhost:3000/health
```

**Ожидаемый ответ (все 3):**
```json
{
  "success": true,
  "message": "API работает",
  "timestamp": "2026-01-25T...",
  "version": "v1"
}
```

---

### Шаг 3: Проверка подключения к БД

```bash
# Проверьте MySQL запущен
sudo systemctl status mysql

# Подключитесь к БД
mysql -u utmn_user -p utmn_security_db

# Проверьте таблицы
SHOW TABLES;

# Проверьте роли
SELECT * FROM roles;

# Выход
EXIT;
```

**Ожидаемые таблицы:**
- `users`
- `roles`
- `students`
- `employees`
- `access_logs`
- `parking_records`
- `storage_items`
- `audit_log`

---

### Шаг 4: Создание тестового пользователя

```bash
# Перейдите в директорию backend
cd /var/www/utmn-security/backend

# Запустите скрипт создания пользователя
node create-test-user.js
```

**Ожидаемый вывод:**
```
✅ Подключено к базе данных
✅ Пароль захеширован
✅ Тестовый пользователь создан!

📋 Данные для входа:
   Username: admin
   Password: Admin2025
   Role: admin
   ID: 1
```

**Если пользователь уже существует:**
```
⚠️  Пользователь с username "admin" уже существует
   ID: 1
✅ Пароль обновлён на: Admin2025
```

---

### Шаг 5: Тестирование авторизации

#### Тест 1: Логин через /api

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Admin2025"}'
```

#### Тест 2: Логин через /v1

```bash
curl -X POST http://localhost:3000/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"Admin2025"}'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expiresIn": 86400,
    "user": {
      "id": 1,
      "username": "admin",
      "fullName": "Администратор Системы",
      "email": "admin@utmn.ru",
      "role": "admin",
      "roleDisplayName": "Администратор",
      "authType": "local",
      "isActive": true,
      "createdAt": "2026-01-25T...",
      "lastLogin": "2026-01-25T..."
    }
  }
}
```

#### Тест 3: Получение информации о пользователе

```bash
# Сохраните токен из предыдущего запроса
TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Запросите информацию о пользователе
curl http://localhost:3000/api/auth/me \
  -H "Authorization: Bearer $TOKEN"
```

---

### Шаг 6: Тестирование в браузере

1. **Откройте приложение в браузере**
   ```
   http://ВАШ_СЕРВЕР:ПОРТ
   ```

2. **Откройте DevTools (F12)**
   - Вкладка: Network
   - Включите "Preserve log"

3. **Попробуйте войти**
   - Username: `admin`
   - Password: `Admin2025`
   - Auth Type: `Local`

4. **Проверьте запросы**
   - Найдите запрос `/v1/auth/login` или `/api/auth/login`
   - Status Code должен быть `200`
   - Response должен содержать `"success": true`

5. **Проверьте токен в localStorage**
   - DevTools → Application → Local Storage
   - Ключ: `auth_token`
   - Значение: JWT токен

---

## 🔍 Диагностика проблем

### Проблема: Backend не запускается

```bash
# Проверьте логи
sudo journalctl -u utmn-security -n 100 --no-pager

# Проверьте конфигурацию
cat /var/www/utmn-security/backend/.env

# Проверьте зависимости
cd /var/www/utmn-security/backend
npm list
```

### Проблема: "Cannot connect to database"

```bash
# Проверьте MySQL
sudo systemctl status mysql

# Проверьте параметры подключения
cat /var/www/utmn-security/backend/.env | grep DB_

# Попробуйте подключиться вручную
mysql -h ХОСТ -u ПОЛЬЗОВАТЕЛЬ -p БАЗА_ДАННЫХ
```

### Проблема: "VALIDATION_ERROR"

```bash
# Проверьте что отправляется
# В DevTools → Network → Request Payload

# Проверьте схему валидации
cat /var/www/utmn-security/backend/src/controllers/auth.controller.js | grep -A 5 "loginSchema"
```

### Проблема: "NOT_FOUND"

```bash
# Проверьте логи backend
sudo journalctl -u utmn-security -f

# Проверьте какой URL используется
# В DevTools → Network → Request URL

# Должно быть:
# ✅ http://localhost:3000/v1/auth/login
# ИЛИ
# ✅ http://localhost:3000/api/auth/login
```

---

## 📊 Доступные endpoints

### Авторизация
- `POST /api/auth/login` - локальная авторизация
- `POST /api/auth/sso` - SSO авторизация
- `POST /api/auth/logout` - выход
- `GET /api/auth/me` - текущий пользователь

### Пользователи
- `GET /api/users` - список пользователей
- `GET /api/users/:id` - пользователь по ID
- `POST /api/users` - создать пользователя
- `PUT /api/users/:id` - обновить пользователя
- `DELETE /api/users/:id` - удалить пользователя
- `GET /api/users/statistics` - статистика

### Роли
- `GET /api/roles` - список ролей

### Студенты
- `GET /api/students` - список студентов
- `GET /api/students/:id` - студент по ID
- `POST /api/students` - создать студента
- `PUT /api/students/:id` - обновить студента
- `DELETE /api/students/:id` - удалить студента

### Сотрудники
- `GET /api/employees` - список сотрудников
- `GET /api/employees/:id` - сотрудник по ID
- `POST /api/employees` - создать сотрудника
- `PUT /api/employees/:id` - обновить сотрудника
- `DELETE /api/employees/:id` - удалить сотрудника

### Логи доступа
- `GET /api/access-logs` - логи доступа

### Парковка
- `GET /api/parking` - записи парковки

### Склад
- `GET /api/storage` - элементы склада

### Аналитика
- `GET /api/analytics/dashboard` - данные дашборда
- `GET /api/analytics/students` - аналитика студентов
- `GET /api/analytics/employees` - аналитика сотрудников

---

## 🔐 Роли и права доступа

### admin (Администратор)
- Полный доступ ко всем функциям системы
- Управление пользователями, ролями
- Просмотр всей статистики и аналитики

### security (Служба безопасности)
- Просмотр логов доступа
- Управление парковкой
- Просмотр информации о студентах/сотрудниках

### teacher (Преподаватель)
- Просмотр информации о студентах
- Ограниченный доступ к статистике

### student (Студент)
- Просмотр своей информации
- Базовые функции

### guest (Гость)
- Минимальный доступ
- Только публичная информация

---

## ✅ Финальная проверка

- [ ] Backend запущен (`systemctl status utmn-security`)
- [ ] MySQL подключена
- [ ] API отвечает на `/health`
- [ ] Тестовый пользователь создан
- [ ] Логин через curl работает
- [ ] Логин в браузере работает
- [ ] Токен сохраняется в localStorage
- [ ] Frontend отображает данные пользователя

---

## 🎉 Готово к эксплуатации!

Система полностью настроена и готова к использованию.

**Данные для входа:**
- Username: `admin`
- Password: `Admin2025`
- Role: `admin`

---

**Версия:** 1.0  
**Дата:** 25.01.2026  
**Статус:** ✅ Готово к production
