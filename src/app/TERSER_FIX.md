# ✅ Исправление проблемы с terser

## 🐛 Проблема

При каждом развертывании приходилось вручную устанавливать `terser`:
```bash
npm install terser
```

## 🔧 Решение

### 1. Добавлен terser в package.json

**Файлы:**
- `/package.json` - корневой
- `/frontend/package.json` - frontend

**Изменения:**
```json
{
  "devDependencies": {
    ...
    "terser": "^5.27.0"
  }
}
```

### 2. Обновлены скрипты развертывания

**`deploy-production.sh`:**
- Теперь **всегда** выполняет `npm install` для frontend
- Обновляет зависимости даже если `node_modules` существует
- Добавлена проверка `package.json`

**`quick-deploy.sh`:**
- Убрано условие проверки `node_modules`
- Всегда выполняется `npm install`

## ✅ Что сделано

### Обновлено:
1. ✅ `/package.json` - добавлен terser
2. ✅ `/frontend/package.json` - добавлен terser  
3. ✅ `/deploy-production.sh` - всегда устанавливает зависимости
4. ✅ `/quick-deploy.sh` - всегда устанавливает зависимости

## 🚀 Проверка

### 1. Проверьте что terser добавлен

```bash
cd frontend
cat package.json | grep terser
```

**Должно вывести:**
```json
"terser": "^5.27.0"
```

### 2. Установите зависимости

```bash
cd frontend
npm install
```

**Должно установиться без ошибок**

### 3. Разверните проект

```bash
cd ..
sudo ./deploy-production.sh
```

или

```bash
sudo ./quick-deploy.sh
```

**Terser должен установиться автоматически!**

## 📝 Дополнительно

### Если нужно обновить зависимости вручную

```bash
cd frontend

# Удалить node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Переустановить
npm install

# Проверить что terser установлен
npm list terser
```

### Проверка версии terser

```bash
cd frontend
npm list terser
```

**Вывод:**
```
utmn-security-frontend@1.0.0
└── terser@5.27.0
```

## 🎯 Результат

Теперь при развертывании:
1. **`npm install`** выполняется автоматически
2. **terser** устанавливается из `package.json`
3. **Не нужно** устанавливать вручную
4. **Всегда** актуальная версия зависимостей

---

**Дата:** 26.01.2026  
**Версия:** 1.0.0
