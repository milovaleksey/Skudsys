# 🎯 Система безопасности инфраструктуры ТюмГУ

## Быстрое развертывание

### Вариант 1: Автоматическое развертывание (рекомендуется)

```bash
chmod +x make-executable.sh && ./make-executable.sh
./deploy-external.sh
```

После установки откройте: **http://ВАШ_IP_АДРЕС**  
Логин: `admin` | Пароль: `admin123`

---

## 📚 Доступные скрипты

| Скрипт | Описание |
|--------|----------|
| `./deploy-external.sh` | Полное развертывание с внешним доступом |
| `./reset-database.sh` | Очистка и пересоздание БД |
| `./check-status.sh` | Проверка статуса всех сервисов |
| `./deploy-http.sh` | Развертывание для работы за reverse proxy |
| `./deploy.sh` | Развертывание с HTTPS |

---

## 📖 Документация

| Файл | Описание |
|------|----------|
| `START_HERE_QUICK.md` | Краткая инструкция (2 команды) |
| `DEPLOY_NOW.md` | Подробная инструкция развертывания |
| `HTTP_REVERSE_PROXY_SETUP.md` | Настройка с reverse proxy |
| `API_ENDPOINTS.md` | Документация API |
| `ROLES.md` | Система ролей и прав доступа |

---

## 🛠️ Требования

- **ОС:** Debian 10+ / Ubuntu 20.04+
- **Node.js:** 16.x или выше
- **MySQL:** 5.7 или 8.0+
- **Nginx:** 1.18+
- **RAM:** минимум 2GB
- **Disk:** минимум 10GB

---

## 🔧 Ручная установка зависимостей

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка базовых пакетов
sudo apt install -y nginx mysql-server git curl

# Установка Node.js 18.x LTS
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка версий
node --version
npm --version
mysql --version
nginx -v
```

---

## 🗄️ Структура базы данных

База данных `utmn_security` включает 11 таблиц:

- `users` - Пользователи системы
- `roles` - Роли и права доступа
- `students` - База студентов
- `employees` - База сотрудников
- `parking_lots` - Парковочные зоны
- `parking_records` - Записи парковки
- `storage_items` - Хранилище вещей
- `access_logs` - Логи доступа
- `user_sessions` - Активные сессии
- `system_settings` - Настройки системы
- `notifications` - Уведомления

---

## 🎨 Функционал системы

### ✅ Реализованные модули

- **Авторизация и аутентификация** - JWT токены, refresh tokens
- **Управление пользователями** - CRUD, смена паролей, активация
- **Система ролей** - 5 встроенных ролей + кастомные
- **База студентов** - Поиск, фильтрация, статистика
- **База сотрудников** - Поиск, фильтрация, статистика  
- **Парковочная система** - 300+ записей, фильтры по ГРЗ/ФИО
- **Конструктор дашбордов** - Drag-and-drop виджеты
- **Логи доступа** - Полная история действий
- **Аналитика** - Графики, отчеты, тренды

### 🎯 Встроенные роли

1. **admin** - Полный доступ ко всем функциям
2. **security** - Служба безопасности
3. **manager** - Менеджер
4. **operator** - Оператор
5. **viewer** - Просмотр данных

---

## 🌐 API Endpoints

### Авторизация
```
POST   /v1/auth/login       - Вход
POST   /v1/auth/logout      - Выход
POST   /v1/auth/refresh     - Обновление токена
GET    /v1/auth/me          - Текущий пользователь
```

### Пользователи
```
GET    /v1/users            - Список пользователей
POST   /v1/users            - Создать пользователя
GET    /v1/users/:id        - Получить пользователя
PUT    /v1/users/:id        - Обновить пользователя
DELETE /v1/users/:id        - Удалить пользователя
```

### Студенты
```
GET    /v1/students         - Список студентов
GET    /v1/students/:id     - Получить студента
GET    /v1/students/statistics - Статистика
```

### Сотрудники
```
GET    /v1/employees        - Список сотрудников
GET    /v1/employees/:id    - Получить сотрудника
GET    /v1/employees/statistics - Статистика
```

### Парковка
```
GET    /v1/parking/lots     - Парковочные зоны
GET    /v1/parking/records  - Записи парковки
GET    /v1/parking/statistics - Статистика
```

Полная документация: `API_ENDPOINTS.md`

---

## 🔒 Безопасность

### Production настройки

1. **Смените все пароли:**
   - Пароль администратора
   - Пароль MySQL
   - JWT секреты

2. **Настройте HTTPS:**
   - Получите SSL сертификат (Let's Encrypt)
   - Настройте reverse proxy с SSL termination

3. **Настройте firewall:**
   ```bash
   sudo ufw allow 22/tcp  # SSH
   sudo ufw allow 80/tcp  # HTTP
   sudo ufw allow 443/tcp # HTTPS
   sudo ufw enable
   ```

4. **Регулярные бэкапы:**
   ```bash
   mysqldump -u utmn_admin -p utmn_security > backup.sql
   ```

---

## 📊 Мониторинг

### Проверка статуса сервисов
```bash
./check-status.sh
```

### Просмотр логов
```bash
# Backend
journalctl -u utmn-backend -f

# Frontend  
journalctl -u utmn-frontend -f

# Nginx
sudo tail -f /var/log/nginx/utmn-security-error.log
```

### Статус systemd
```bash
sudo systemctl status utmn-backend utmn-frontend nginx mysql
```

---

## 🐛 Troubleshooting

### Backend не запускается
```bash
journalctl -u utmn-backend -n 100 --no-pager
sudo systemctl restart utmn-backend
```

### Nginx ошибки
```bash
sudo nginx -t
sudo tail -f /var/log/nginx/error.log
sudo systemctl restart nginx
```

### MySQL проблемы
```bash
sudo systemctl status mysql
mysql -u utmn_admin -putmn_admin123 utmn_security -e "SHOW TABLES;"
```

### Проверка портов
```bash
sudo netstat -tulpn | grep -E ':(80|3000|5173|3306)'
```

---

## 📦 Production Build

После отладки системы:

```bash
# 1. Сборка frontend
npm run build

# 2. Обновите Nginx конфиг
sudo nano /etc/nginx/sites-available/utmn-security

# Раскомментируйте:
# root /var/www/utmn-security/dist;
# try_files $uri $uri/ /index.html;

# И закомментируйте:
# proxy_pass http://localhost:5173;

# 3. Перезапустите Nginx
sudo systemctl restart nginx

# 4. Остановите dev сервер
sudo systemctl stop utmn-frontend
sudo systemctl disable utmn-frontend
```

---

## 🔄 Обновление системы

```bash
cd /var/www/utmn-security

# Бэкап БД
./backup-database.sh

# Обновление кода
git pull

# Обновление зависимостей
npm install
cd backend && npm install && cd ..

# Перезапуск сервисов
sudo systemctl restart utmn-backend utmn-frontend
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `journalctl -u utmn-backend -f`
2. Проверьте статус: `./check-status.sh`
3. Проверьте конфигурацию: `sudo nginx -t`
4. Проверьте БД: `mysql -u utmn_admin -putmn_admin123 utmn_security`

---

## 📝 Лицензия

Proprietary - ТюмГУ © 2024

---

## 🎯 Быстрые ссылки

- [Быстрый старт](START_HERE_QUICK.md)
- [Подробное развертывание](DEPLOY_NOW.md)
- [API документация](API_ENDPOINTS.md)
- [Система ролей](ROLES.md)
- [Настройка reverse proxy](HTTP_REVERSE_PROXY_SETUP.md)

---

**Версия:** 1.0  
**Последнее обновление:** 2024  
**Статус:** Production Ready ✅
