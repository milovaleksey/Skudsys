# 🚀 Старт работы - Система безопасности ТюмГУ

## 📋 Что это?

Веб-приложение для управления безопасностью инфраструктуры ТюмГУ:
- **5 типов ролей** (admin, security, manager, operator, viewer)
- **Управление** студентами, сотрудниками
- **Мониторинг** проходов, парковки, хранилища
- **Аналитика** и отчеты
- **JWT аутентификация** + SSO

---

## ⚡ Быстрый старт

### 1️⃣ Production развертывание (5 минут)

```bash
# Сделайте скрипты исполняемыми
chmod +x make-executable.sh
./make-executable.sh

# Запустите автоматическую установку
sudo ./deploy-quick.sh
```

**Готово!** Откройте: `http://<IP-адрес-сервера>`

**Тестовый вход:**
- Логин: `admin_security`
- Пароль: `test123`

⚠️ Смените пароль после входа!

---

### 2️⃣ Development режим (локальная разработка)

```bash
# Сделайте скрипты исполняемыми
chmod +x make-executable.sh
./make-executable.sh

# Запустите в dev режиме
./start-dev.sh
```

Откройте: `http://localhost:5173`

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| [QUICKSTART.md](./QUICKSTART.md) | Быстрый старт (5 минут) |
| [DEPLOYMENT.md](./DEPLOYMENT.md) | Полное руководство по развертыванию |
| [README.md](./README.md) | Общая информация о проекте |
| [BUILD.md](./BUILD.md) | Инструкции по сборке |
| [TEST_ACCOUNTS.md](./TEST_ACCOUNTS.md) | Тестовые учетные записи |
| [API_DOCUMENTATION.md](./API_DOCUMENTATION.md) | API документация |

---

## 🛠️ Полезные скрипты

### Основные

```bash
./deploy-quick.sh       # Быстрое развертывание production
./start-dev.sh          # Запуск в режиме разработки
./verify-deployment.sh  # Проверка развертывания
```

### Дополнительные

```bash
./deploy.sh             # Полная установка с HTTPS
./deploy-http.sh        # Установка только HTTP
./check-status.sh       # Статус всех сервисов
./reset-database.sh     # Пересоздание базы данных
```

---

## 🔧 Управление сервисами

### Backend

```bash
# Статус
sudo systemctl status utmn-security

# Перезапуск
sudo systemctl restart utmn-security

# Логи
sudo journalctl -u utmn-security -f
```

### Nginx

```bash
# Статус
sudo systemctl status nginx

# Перезагрузка
sudo systemctl reload nginx

# Логи
sudo tail -f /var/log/nginx/utmn-security-*.log
```

### База данных

```bash
# Подключение
mysql -u utmn_user -p utmn_security

# Резервная копия
mysqldump -u utmn_user -p utmn_security > backup.sql
```

---

## 🌐 Доступ из сети

### Узнать IP-адрес

```bash
hostname -I | awk '{print $1}'
```

### Открыть порты

```bash
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw enable
```

### Проверка

```bash
# Health check
curl http://localhost/health

# Из другого компьютера
curl http://<IP-адрес>/health
```

---

## 🎭 Роли пользователей

| Роль | Доступ |
|------|--------|
| **admin** | Полный доступ ко всем функциям |
| **security** | Проходы, парковка, хранилище |
| **manager** | Аналитика, отчеты |
| **operator** | Просмотр данных |
| **viewer** | Только dashboard |

---

## 🐛 Проблемы?

### Backend не запускается

```bash
sudo journalctl -u utmn-security -n 50
sudo systemctl restart utmn-security
```

### Frontend не открывается

```bash
sudo systemctl restart nginx
sudo nginx -t
```

### MySQL ошибки

```bash
sudo systemctl status mysql
mysql -u utmn_user -p utmn_security -e "SELECT 1;"
```

Больше решений: [DEPLOYMENT.md - Решение проблем](./DEPLOYMENT.md#решение-проблем)

---

## 📞 Следующие шаги

1. ✅ Разверните систему
2. ✅ Войдите как администратор
3. ✅ Смените пароль
4. ✅ Создайте пользователей
5. ✅ Настройте HTTPS (для production)
6. ✅ Изучите возможности

---

## 📦 Требования

**Минимальные:**
- Debian 11/12 или Ubuntu 20.04+
- Node.js 18+ (рекомендуется 20.x)
- MySQL 8.0+
- Nginx 1.18+
- 4 GB RAM
- 20 GB диск

---

## 🎨 Фирменный стиль ТюмГУ

**Цвет:** `#00aeef` 🎨

---

**Версия:** 2.0  
**Дата:** 25.01.2026  
**ТюмГУ - Система безопасности инфраструктуры**