# 📦 Инструкция по установке и запуску системы безопасности ТюмГУ

## 📋 Содержание

1. [Требования к системе](#-требования-к-системе)
2. [Установка зависимостей](#-установка-зависимостей)
3. [Загрузка проекта](#-загрузка-проекта)
4. [Настройка базы данных](#-настройка-базы-данных)
5. [Настройка Backend](#-настройка-backend)
6. [Настройка Frontend](#-настройка-frontend)
7. [Первый запуск](#-первый-запуск)
8. [Проверка работы](#-проверка-работы)

---

## ✅ Требования к системе

### Минимальные требования:

- **ОС:** Windows 10/11, macOS 10.15+, Ubuntu 20.04+ или другой Linux
- **RAM:** 4 GB (рекомендуется 8 GB)
- **Диск:** 2 GB свободного места
- **Процессор:** Любой современный (x64)

### Необходимое ПО:

- ✅ Node.js 18+ и npm
- ✅ MySQL 8.0+
- ✅ Git (опционально, для клонирования)

---

## 🔧 Установка зависимостей

### Шаг 1: Установка Node.js

#### Windows:

1. Скачайте установщик: https://nodejs.org/
2. Выберите версию **LTS** (например, 20.11.0)
3. Запустите установщик
4. Следуйте инструкциям (оставьте все по умолчанию)
5. Перезапустите терминал

**Проверка:**
```cmd
node --version
npm --version
```

Должно быть: `v18.x.x` или выше

#### macOS:

**Через Homebrew (рекомендуется):**
```bash
# Установить Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить Node.js
brew install node
```

**Или скачать с сайта:** https://nodejs.org/

**Проверка:**
```bash
node --version
npm --version
```

#### Linux (Ubuntu/Debian):

```bash
# Обновить систему
sudo apt update

# Установить Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка
node --version
npm --version
```

---

### Шаг 2: Установка MySQL

#### Windows:

1. Скачайте MySQL Installer: https://dev.mysql.com/downloads/installer/
2. Выберите **"mysql-installer-community"**
3. Запустите установщик
4. Выберите **"Developer Default"**
5. Установите пароль для пользователя `root`
6. **ЗАПОМНИТЕ ПАРОЛЬ!** (например: `root123456`)
7. Завершите установку

**Проверка:**
```cmd
mysql --version
```

**Запуск службы MySQL:**
```cmd
net start MySQL80
```

#### macOS:

**Через Homebrew:**
```bash
# Установить MySQL
brew install mysql

# Запустить MySQL
brew services start mysql

# Настроить безопасность
mysql_secure_installation
```

**Проверка:**
```bash
mysql --version
mysql -u root -p
```

#### Linux (Ubuntu/Debian):

```bash
# Установить MySQL
sudo apt update
sudo apt install mysql-server

# Запустить MySQL
sudo systemctl start mysql
sudo systemctl enable mysql

# Настроить безопасность
sudo mysql_secure_installation

# Проверка
mysql --version
```

---

## 📥 Загрузка проекта

### Вариант 1: Клонирование через Git

```bash
# Клонировать репозиторий
git clone <URL_репозитория>

# Перейти в папку
cd utmn-security
```

### Вариант 2: Загрузка ZIP архива

1. Скачайте ZIP архив проекта
2. Распакуйте в удобную папку (например: `C:\Projects\utmn-security` или `~/Projects/utmn-security`)
3. Откройте терминал в этой папке

**Windows:**
- Зажмите `Shift` + правая кнопка мыши → "Открыть PowerShell здесь"

**macOS/Linux:**
- Откройте терминал и перейдите: `cd /path/to/utmn-security`

---

## 🗄️ Настройка базы данных

### Шаг 1: Подключение к MySQL

**Windows:**
```cmd
mysql -u root -p
```

**macOS/Linux:**
```bash
mysql -u root -p
```

Введите пароль root, который вы установили при установке MySQL.

---

### Шаг 2: Создание базы данных

В MySQL консоли выполните:

```sql
-- Создать базу данных
CREATE DATABASE utmn_security CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Создать пользователя (опционально)
CREATE USER 'utmn_user'@'localhost' IDENTIFIED BY 'utmn_password_2026';

-- Выдать права
GRANT ALL PRIVILEGES ON utmn_security.* TO 'utmn_user'@'localhost';

-- Применить изменения
FLUSH PRIVILEGES;

-- Проверить
SHOW DATABASES;

-- Выйти
EXIT;
```

**Ожидаемый результат:**
```
+--------------------+
| Database           |
+--------------------+
| utmn_security      |
| ...                |
+--------------------+
```

---

### Шаг 3: Инициализация таблиц

#### Вариант 1: Через скрипт (автоматически)

**Windows:**
```cmd
cd backend
node src/scripts/initDatabase.js
```

**macOS/Linux:**
```bash
cd backend
node src/scripts/initDatabase.js
```

#### Вариант 2: Вручную через SQL файл

```bash
# Импортировать структуру
mysql -u root -p utmn_security < backend/database/schema.sql

# Импортировать данные
mysql -u root -p utmn_security < backend/database/seed.sql
```

---

### Шаг 4: Проверка таблиц

Подключитесь к MySQL:
```bash
mysql -u root -p utmn_security
```

Проверьте таблицы:
```sql
SHOW TABLES;
```

**Ожидаемый результат (11 таблиц):**
```
+---------------------------+
| Tables_in_utmn_security   |
+---------------------------+
| access_logs               |
| dormitories               |
| employees                 |
| foreign_students          |
| parking                   |
| permission_types          |
| role_permissions          |
| roles                     |
| storage                   |
| students                  |
| users                     |
+---------------------------+
11 rows in set
```

Выйдите:
```sql
EXIT;
```

---

## ⚙️ Настройка Backend

### Шаг 1: Перейти в папку backend

```bash
cd backend
```

### Шаг 2: Установить зависимости

```bash
npm install
```

Это займет 2-5 минут. Установится ~500 пакетов.

**Ожидаемый вывод:**
```
added 500 packages in 2m
```

---

### Шаг 3: Создать файл .env

**Windows:**
```cmd
copy .env.example .env
```

**macOS/Linux:**
```bash
cp .env.example .env
```

---

### Шаг 4: Настроить .env файл

Откройте файл `backend/.env` в текстовом редакторе:

**Windows:** Notepad, VS Code, или любой редактор
**macOS:** TextEdit, VS Code, nano
**Linux:** nano, vim, VS Code

**Измените следующие параметры:**

```env
# ===================================
# База данных MySQL
# ===================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=utmn_security
DB_USER=root
DB_PASSWORD=ВАШ_ПАРОЛЬ_MYSQL_ЗДЕСЬ

# ===================================
# JWT Authentication
# ===================================
JWT_SECRET=utmn_security_secret_key_2026_minimum_32_characters
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# ===================================
# Сервер
# ===================================
NODE_ENV=development
PORT=3000
API_VERSION=v1

# HTTPS (пока отключаем)
USE_HTTPS=false

# ===================================
# CORS
# ===================================
CORS_ORIGIN=http://localhost:5173
CORS_CREDENTIALS=true

# ===================================
# Безопасность
# ===================================
PASSWORD_MIN_LENGTH=12
LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCK_TIME=15
```

**Важно:**
- Замените `DB_PASSWORD` на ваш реальный пароль MySQL
- Сохраните файл

---

### Шаг 5: Проверка конфигурации

Проверьте подключение к БД:

```bash
node src/config/database.js
```

Или запустите тестовый скрипт:
```bash
node -e "require('./src/config/database').connectDatabase().then(() => console.log('✅ Подключение успешно')).catch(err => console.error('❌ Ошибка:', err.message))"
```

**Ожидаемый результат:**
```
✅ Подключение успешно
```

---

## 🎨 Настройка Frontend

### Шаг 1: Вернуться в корневую папку

```bash
cd ..
```

Вы должны быть в корне проекта (где находится `package.json`).

---

### Шаг 2: Установить зависимости

```bash
npm install
```

Это займет 2-5 минут.

**Ожидаемый вывод:**
```
added 300 packages in 2m
```

---

### Шаг 3: Создать .env файл (опционально)

Frontend обычно не требует .env, но если нужно:

**Windows:**
```cmd
echo VITE_API_URL=http://localhost:3000/v1 > .env
```

**macOS/Linux:**
```bash
echo "VITE_API_URL=http://localhost:3000/v1" > .env
```

---

## 🚀 Первый запуск

### Вариант 1: Автоматический запуск (рекомендуется)

#### Windows:
```cmd
start.bat
```

#### Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```

Скрипт автоматически:
- ✅ Проверит зависимости
- ✅ Запустит Backend в отдельном окне
- ✅ Запустит Frontend в отдельном окне

---

### Вариант 2: Ручной запуск

#### Терминал 1 - Backend:

```bash
# Перейти в папку backend
cd backend

# Запустить сервер
npm run dev
```

**Ожидаемый вывод:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 HTTP сервер запущен
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 Порт:    3000
📡 API:     http://localhost:3000/v1
🏥 Health:  http://localhost:3000/health
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**НЕ ЗАКРЫВАЙТЕ ЭТО ОКНО!**

---

#### Терминал 2 - Frontend:

Откройте **НОВЫЙ** терминал (не закрывая первый):

```bash
# Убедитесь, что вы в корне проекта
npm run dev
```

**Ожидаемый вывод:**
```
  VITE v5.0.0  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

**НЕ ЗАКРЫВАЙТЕ ЭТО ОКНО!**

---

### Открыть приложение

Откройте браузер и перейдите:

```
http://localhost:5173
```

---

## ✅ Проверка работы

### 1. Проверка Backend

Откройте в браузере или через curl:

```bash
curl http://localhost:3000/health
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "API работает",
  "timestamp": "2026-01-20T12:00:00.000Z",
  "version": "v1",
  "https": false
}
```

---

### 2. Проверка Frontend

Откройте: http://localhost:5173

Должна открыться страница входа в систему.

---

### 3. Тестовый вход

Используйте тестовые учетные данные:

**Администратор:**
- Логин: `admin_security`
- Пароль: `AdminSecure2024!`

**Безопасность:**
- Логин: `security_operator`
- Пароль: `SecureOp2024!`

**Менеджер:**
- Логин: `manager_analytics`
- Пароль: `Manager2024!`

---

### 4. Проверка базы данных

Подключитесь к MySQL:
```bash
mysql -u root -p utmn_security
```

Проверьте пользователей:
```sql
SELECT user_id, username, full_name, role FROM users;
```

**Ожидаемый результат:**
```
+---------+-------------------+------------------------+-------------+
| user_id | username          | full_name              | role        |
+---------+-------------------+------------------------+-------------+
|       1 | admin_security    | Администратор системы  | admin       |
|       2 | security_operator | Оператор безопасности  | security    |
|       3 | manager_analytics | Менеджер аналитики     | manager     |
+---------+-------------------+------------------------+-------------+
```

---

## 🎯 Итоговая проверка - Чеклист

Отметьте выполненные пункты:

- [ ] Node.js установлен (`node --version`)
- [ ] MySQL установлен и запущен (`mysql --version`)
- [ ] База данных создана (`utmn_security`)
- [ ] Таблицы созданы (11 таблиц)
- [ ] Backend зависимости установлены (`backend/node_modules/`)
- [ ] Frontend зависимости установлены (`node_modules/`)
- [ ] Backend `.env` настроен (пароль БД указан)
- [ ] Backend запущен (http://localhost:3000/health работает)
- [ ] Frontend запущен (http://localhost:5173 открывается)
- [ ] Вход работает (можно войти как `admin_security`)
- [ ] Данные загружаются (видны студенты/сотрудники)

---

## 🐛 Решение проблем

### ❌ Ошибка: "Cannot find module"

**Причина:** Зависимости не установлены

**Решение:**
```bash
# Backend
cd backend
npm install

# Frontend
cd ..
npm install
```

---

### ❌ Ошибка: "ECONNREFUSED 127.0.0.1:3306"

**Причина:** MySQL не запущен

**Решение:**

**Windows:**
```cmd
net start MySQL80
```

**macOS:**
```bash
brew services start mysql
```

**Linux:**
```bash
sudo systemctl start mysql
```

---

### ❌ Ошибка: "Access denied for user 'root'@'localhost'"

**Причина:** Неправильный пароль в `.env`

**Решение:**

1. Откройте `backend/.env`
2. Проверьте `DB_PASSWORD`
3. Убедитесь, что это ваш реальный пароль MySQL
4. Сохраните файл
5. Перезапустите backend

---

### ❌ Ошибка: "Database 'utmn_security' does not exist"

**Причина:** База данных не создана

**Решение:**
```bash
mysql -u root -p
```

```sql
CREATE DATABASE utmn_security CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

Затем инициализируйте таблицы:
```bash
cd backend
node src/scripts/initDatabase.js
```

---

### ❌ Ошибка: "Port 3000 is already in use"

**Причина:** Порт занят другим процессом

**Решение:**

**Windows:**
```cmd
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

**macOS/Linux:**
```bash
lsof -ti:3000 | xargs kill -9
```

Или измените порт в `backend/.env`:
```env
PORT=3001
```

---

### ❌ Ошибка: "Port 5173 is already in use"

**Решение:**

**Windows:**
```cmd
netstat -ano | findstr :5173
taskkill /PID <PID> /F
```

**macOS/Linux:**
```bash
lsof -ti:5173 | xargs kill -9
```

---

### ❌ Frontend открывается, но "Cannot connect to server"

**Причина:** Backend не запущен или API URL неправильный

**Решение:**

1. Проверьте Backend:
```bash
curl http://localhost:3000/health
```

2. Если не работает, перезапустите Backend:
```bash
cd backend
npm run dev
```

3. Проверьте консоль браузера (F12) на ошибки

---

### ❌ "Invalid credentials" при входе

**Причина:** Данные не загружены в БД

**Решение:**

Загрузите тестовые данные:
```bash
cd backend
node src/scripts/initDatabase.js
```

Или проверьте пользователей:
```bash
mysql -u root -p utmn_security
```

```sql
SELECT username, role FROM users;
```

---

## 📞 Дополнительная помощь

### Логи Backend

В терминале где запущен Backend вы увидите все запросы:
```
GET /health 200 5ms
POST /v1/auth/login 200 150ms
```

### Логи Frontend

Откройте консоль браузера (F12) → вкладка "Console"

### Проверка сети

В браузере (F12) → вкладка "Network" → посмотрите запросы к API

---

## 🎉 Готово!

Если все пункты выполнены, система запущена и готова к использованию!

### Что дальше?

1. Изучите интерфейс
2. Попробуйте разные роли (admin, security, manager)
3. Добавьте студентов/сотрудников
4. Посмотрите логи доступа
5. Настройте систему под ваши нужды

### Полезные команды:

```bash
# Остановить серверы: Ctrl+C в каждом терминале

# Перезапустить Backend
cd backend
npm run dev

# Перезапустить Frontend
npm run dev

# Очистить кеш и переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

---

**Версия:** 1.0  
**Дата:** 20.01.2026  
**ТюмГУ - Системы безопасности инфраструктуры**
