# ⚡ Быстрая установка (без HTTPS)

## 📦 Требования

✅ Node.js 18+ и npm  
✅ MySQL 8.0+  
✅ Git (опционально)

---

## 🚀 Установка за 5 шагов

### 1️⃣ Установить зависимости

**Node.js:** https://nodejs.org/ (LTS версия)  
**MySQL:** https://dev.mysql.com/downloads/

**Проверка:**
```bash
node --version  # должно быть v18+
mysql --version
```

---

### 2️⃣ Создать базу данных

```bash
# Подключиться к MySQL
mysql -u root -p

# В MySQL консоли:
CREATE DATABASE utmn_security CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

---

### 3️⃣ Настроить Backend

```bash
# Перейти в папку backend
cd backend

# Установить зависимости
npm install

# Создать .env файл
cp .env.example .env

# Отредактировать .env (указать пароль MySQL)
# DB_PASSWORD=ваш_пароль_mysql

# Инициализировать таблицы
node src/scripts/initDatabase.js
```

---

### 4️⃣ Настроить Frontend

```bash
# Вернуться в корень
cd ..

# Установить зависимости
npm install
```

---

### 5️⃣ Запустить

**Автоматически:**

```bash
# Linux/Mac
chmod +x start.sh
./start.sh

# Windows
start.bat
```

**Или вручную (2 терминала):**

Терминал 1 (Backend):
```bash
cd backend
npm run dev
```

Терминал 2 (Frontend):
```bash
npm run dev
```

---

## 🌐 Открыть приложение

```
http://localhost:5173
```

**Тестовый вход:**
- Логин: `admin_security`
- Пароль: `AdminSecure2024!`

---

## ✅ Проверка

```bash
# Backend работает?
curl http://localhost:3000/health

# Должен вернуть:
# {"success":true,"message":"API работает"}
```

---

## 🐛 Проблемы?

### MySQL не запущен:
```bash
# Windows
net start MySQL80

# Mac
brew services start mysql

# Linux
sudo systemctl start mysql
```

### Порт занят:
```bash
# Убить процесс на порту 3000
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Mac/Linux
lsof -ti:3000 | xargs kill -9
```

### Не подключается к БД:
1. Проверьте пароль в `backend/.env`
2. Проверьте что MySQL запущен: `mysql -u root -p`

---

## 📚 Подробная инструкция

См. **[INSTALLATION_GUIDE.md](INSTALLATION_GUIDE.md)** для детального руководства.

---

## 🎯 Структура после установки

```
utmn-security/
├── backend/
│   ├── node_modules/     ✅ Установлено
│   ├── .env              ✅ Настроено (пароль MySQL)
│   └── ...
├── node_modules/         ✅ Установлено
├── package.json
└── ...
```

**База данных:**
- Название: `utmn_security`
- Таблиц: 11
- Тестовых пользователей: 5

---

**Готово! 🎉**

**Версия:** 1.0  
**Дата:** 20.01.2026
