# 🔐 SSL сертификаты и Nginx - Полное руководство

## 🎯 Что создано

### 1. Генерация сертификатов ✅
- `/scripts/generate-ssl-cert.sh` - Linux/Mac
- `/scripts/generate-ssl-cert.bat` - Windows

### 2. Конфигурации Nginx ✅
- `/nginx/utmn-security.conf` - Production (HTTPS)
- `/nginx/utmn-security-dev.conf` - Development (HTTP)

### 3. Установка Nginx ✅
- `/scripts/setup-nginx.sh` - автоматическая установка

### 4. Запуск с SSL ✅
- `/start-https.sh` - Linux/Mac
- `/start-https.bat` - Windows

### 5. Документация ✅
- `/NGINX_GUIDE.md` - полное руководство по Nginx
- `/SSL_GUIDE.md` - руководство по SSL
- `/FIX_SSL_ERROR.md` - исправление вашей ошибки

---

## 🚀 Решение вашей проблемы

### Ошибка:
```
cannot load certificate "/etc/ssl/certs/utmn-security.crt": 
BIO_new_file() failed (SSL: error:80000002:system library::No such file or directory)
```

### Решение за 3 команды:

```bash
# 1. Сгенерировать сертификаты
chmod +x scripts/generate-ssl-cert.sh
./scripts/generate-ssl-cert.sh

# 2. Установить в Nginx
chmod +x scripts/setup-nginx.sh
sudo ./scripts/setup-nginx.sh

# 3. Готово!
curl -k https://localhost/health
```

---

## 📖 Варианты использования

### Вариант 1: Без Nginx (проще)

**Запуск приложения с встроенным HTTPS:**

```bash
# Генерация сертификатов
./scripts/generate-ssl-cert.sh

# Запуск
./start-https.sh
```

**URL:**
- Frontend: https://localhost:5173
- Backend: https://localhost:3443

### Вариант 2: С Nginx (production-like)

**Запуск через Nginx reverse proxy:**

```bash
# 1. Генерация сертификатов
./scripts/generate-ssl-cert.sh

# 2. Установка Nginx и сертификатов
sudo ./scripts/setup-nginx.sh

# 3. Запуск backend и frontend
cd backend && npm run dev &
npm run dev &

# 4. Nginx проксирует запросы
```

**URL:**
- Все через Nginx: https://localhost

### Вариант 3: Без SSL (разработка)

**Простой HTTP режим:**

```bash
./start.sh
```

**URL:**
- Frontend: http://localhost:5173
- Backend: http://localhost:3000

---

## 🗂️ Структура сертификатов

### В проекте (для разработки)
```
certs/
├── server.key      # Приватный ключ
├── server.crt      # Публичный сертификат
├── server.pem      # Комбинация
└── README.md       # Документация
```

### В системе (для Nginx)
```
/etc/ssl/
├── certs/
│   └── utmn-security.crt      # Публичный сертификат
└── private/
    └── utmn-security.key      # Приватный ключ (600)
```

---

## 🌐 Архитектура с Nginx

```
┌─────────────┐
│   Браузер   │
└──────┬──────┘
       │ HTTPS (443)
       │
┌──────▼───────────────────────────┐
│         Nginx (Reverse Proxy)     │
│  - SSL Termination                │
│  - Load Balancing                 │
│  - Static Files (production)      │
└──────┬───────────────────┬───────┘
       │                   │
       │ HTTP (5173)       │ HTTP (3000)
       │                   │
┌──────▼──────┐    ┌──────▼──────┐
│  Frontend   │    │   Backend   │
│  (Vite dev  │    │  (Node.js)  │
│   server)   │    │             │
└─────────────┘    └─────────────┘
```

---

## 📋 Быстрый старт

### Для отладки (БЕЗ Nginx):

```bash
# 1. Сгенерировать сертификаты
./scripts/generate-ssl-cert.sh

# 2. Запустить с HTTPS
./start-https.sh

# 3. Открыть
# https://localhost:5173
```

### Для production-like (С Nginx):

```bash
# 1. Сгенерировать сертификаты
./scripts/generate-ssl-cert.sh

# 2. Установить Nginx
sudo ./scripts/setup-nginx.sh

# 3. Запустить backend и frontend
cd backend && npm run dev
# В другом терминале:
npm run dev

# 4. Открыть
# https://localhost
```

---

## 🔧 Настройка Backend для Nginx

### .env конфигурация

**Если используете Nginx:**
```env
# Backend работает на HTTP (Nginx добавит HTTPS)
PORT=3000
USE_HTTPS=false

# CORS должен разрешить запросы от Nginx
CORS_ORIGIN=https://localhost,http://localhost
```

**Если БЕЗ Nginx:**
```env
# Backend сам обрабатывает HTTPS
PORT=3000
HTTPS_PORT=3443
USE_HTTPS=true

CORS_ORIGIN=https://localhost:5173
```

---

## 📚 Документация

### Основные руководства:

1. **[FIX_SSL_ERROR.md](FIX_SSL_ERROR.md)** ⭐
   - Исправление вашей ошибки
   - Пошаговое решение
   - Чеклист проверки

2. **[NGINX_GUIDE.md](NGINX_GUIDE.md)**
   - Полное руководство по Nginx
   - Конфигурация и настройка
   - Управление и мониторинг
   - Решение проблем

3. **[SSL_GUIDE.md](SSL_GUIDE.md)**
   - Генерация сертификатов
   - Установка в систему
   - Использование HTTPS
   - Безопасность

4. **[QUICK_START.md](QUICK_START.md)**
   - Быстрый старт системы
   - Базовая настройка

---

## 🎯 Рекомендации

### Для разработки (отладки):

✅ **Используйте встроенный HTTPS сервер** (БЕЗ Nginx)
- Проще настроить
- Меньше компонентов
- Быстрее перезапуск

```bash
./start-https.sh
```

### Для тестирования production:

✅ **Используйте Nginx**
- Близко к production окружению
- Тестирование reverse proxy
- Проверка CORS и заголовков

```bash
sudo ./scripts/setup-nginx.sh
```

### Для CI/CD:

✅ **Используйте обычный HTTP**
- Не нужны сертификаты
- Проще автоматизация

```bash
./start.sh
```

---

## 🔒 Безопасность

### ⚠️ Важно!

**Самоподписанные сертификаты ТОЛЬКО для разработки!**

- ❌ **НЕ** используйте в production
- ❌ **НЕ** коммитьте `*.key` файлы
- ❌ **НЕ** передавайте приватные ключи

### ✅ Для production используйте:

**Let's Encrypt (бесплатно):**
```bash
sudo certbot --nginx -d your-domain.com
```

**Или платные CA сертификаты:**
- DigiCert
- Sectigo
- GlobalSign

---

## 🧪 Тестирование

### Проверка сертификатов:

```bash
# Информация о сертификате
openssl x509 -in certs/server.crt -text -noout

# Проверка ключа
openssl rsa -in certs/server.key -check

# Проверка совпадения
openssl x509 -noout -modulus -in certs/server.crt | openssl md5
openssl rsa -noout -modulus -in certs/server.key | openssl md5
# MD5 должны совпадать!
```

### Проверка Nginx:

```bash
# Конфигурация
sudo nginx -t

# Статус
sudo systemctl status nginx

# Логи
sudo tail -f /var/log/nginx/error.log
```

### Проверка работы:

```bash
# Backend через Nginx
curl -k https://localhost/health

# Frontend через Nginx
curl -k https://localhost/

# Прямой доступ к backend
curl -k https://localhost:3443/health

# Прямой доступ к frontend
curl -k https://localhost:5173/
```

---

## 📊 Сравнение вариантов

| Характеристика | Без Nginx | С Nginx | HTTP только |
|----------------|-----------|---------|-------------|
| Сложность настройки | ⭐⭐ | ⭐⭐⭐⭐ | ⭐ |
| HTTPS | ✅ | ✅ | ❌ |
| Production-like | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐ |
| Скорость запуска | Быстро | Медленно | Очень быстро |
| Для отладки | ✅ | ❌ | ✅✅ |
| Для тестирования | ✅ | ✅✅ | ✅ |
| Требует root | ❌ | ✅ | ❌ |

### Рекомендации:

- **Отладка кода:** HTTP только (`./start.sh`)
- **Тестирование SSL:** Без Nginx (`./start-https.sh`)
- **Финальное тестирование:** С Nginx (`sudo ./scripts/setup-nginx.sh`)

---

## 🎉 Итого

Вы получили:

1. ✅ **Скрипты генерации сертификатов** (Linux/Mac/Windows)
2. ✅ **Конфигурации Nginx** (dev + production)
3. ✅ **Автоматическую установку** Nginx и сертификатов
4. ✅ **Скрипты запуска** с HTTPS
5. ✅ **Полную документацию** (3 руководства)
6. ✅ **Решение вашей ошибки** (FIX_SSL_ERROR.md)

---

## 🚀 Следующие шаги

### Для исправления вашей ошибки:

```bash
# Прочитайте
cat FIX_SSL_ERROR.md

# Выполните
./scripts/generate-ssl-cert.sh
sudo ./scripts/setup-nginx.sh

# Проверьте
sudo nginx -t
curl -k https://localhost/health
```

### Для разработки без Nginx:

```bash
# Просто запустите
./start-https.sh

# Откройте
# https://localhost:5173
```

---

**Готово к отладке с самоподписанными сертификатами! 🔐**

---

**Версия:** 1.0  
**Дата:** 20.01.2026  
**ТюмГУ - Системы безопасности инфраструктуры**
